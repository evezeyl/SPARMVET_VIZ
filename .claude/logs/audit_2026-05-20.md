# Audit Log — 2026-05-20

**Branch:** dev
**Mode:** @dasharch
**Persona effort tags used this session:** opus/high (ADR-082 authoring + BLUEPRINT feature lock)

---

## Session — BLUEPRINT Full Feature Set Lock (ADR-082)

### Trigger

Asked to start BP-FORMS-1 `[opus/high]` (form builder). `@sync` discrepancy surfaced immediately:
`tasks.md` marked BP-FORMS-1 as `[ ]` not-done, but the form renderer was already built and
committed (`ab113c3`, 2026-05-09 — that commit even said "Marked BP-FORMS-1 as completed"). A
later edit reverted it to `[ ]`, re-tagged `opus/high`, and added a "Depends on ACTION-UISCHEMA-1"
framing. User confirmed scope ("verify + implement ADR-075 gaps") and then flagged the Blueprint
functioning had been **redefined** — so the verification became design-reconciliation.

### Verification findings (against the running implementation)

- **Action forms WORK.** Headless render of all **60** transformer action forms via
  `WrangleStudio._render_action_form` → **zero failures**, all 8 widget types handled. Widget
  usage: column_selector 64, string 51, enum 12, number 9, column_or_literal 8, dtype_picker 1,
  expression 1, bool 1. (`tmpAI/verify_bp_forms.py`)
- **4 gaps vs the redefined design:**
  1. Add-node uses a primitive Focus-tab UI; rich 8-widget form is wired only for *editing* (right sidebar). Not unified.
  2. Plot/component nodes have **no form path**; only **7/191** components carry `ui_schema`.
  3. enum `preview` ignored by the renderer (ADR-075 §2).
  4. `expression` widget (only `mutate.expression`) is a plain textarea — no column autocomplete (ADR-075 §2).
- BP-UNDO-1 / BP-ESCAPE-1 / BP-HELP-1 implemented (code present); functional smoke still pending (→ BP-SMOKE-1).
- Minor: TubeMap legend uses forbidden Bootstrap colours (`#0d6efd`, `#198754`).
- Design drift: features landed May 10–11 (Action Parity picker, Field Gap Analysis, Forward-prop hints, Visual Fork) *after* the design docs were last touched; ADR-082 (umbrella lock) was researched but never authored. BLUEPRINT.md "Current State" was stale.

### Decisions (ADR-082, with Eve)

- **Q1 preview** = on-Apply (ratified). **Q2 T3** = manifest-only (BLUEPRINT never authors T3 — hard invariant).
  **Q4 comments** = lost on form round-trip (documented limitation, MVP). **Q5 join** = dedicated Joint Designer pane.
  **Q6 group/plot** = sidebar inventory (MVP) → context menu (v2). **Q7 inspection** = glimpse only.
- **NEW: manifest-draft autosave** (BP-AUTOSAVE-1) — persist in-progress draft + undo history, modeled on HOME ghost-save.
- **AI assistant** = advise-only, never blocking, Socratic nudging on bad practice. Definition file already exists
  (`config/ui/agents/blueprint_default.md`).
- **Q3 branch (the key terminology fix)** — **branch = lineage bifurcation at a node**: upstream shared
  (Tier 1 trunk materialized once, branches `scan_parquet` it), child lineage diverges downstream as a
  fragment-per-component `!include` (canonical `Summary` / `Summary_quality`). This is the Bifurcation Point
  Rule made interactive. NOT whole-manifest duplication (rare, new-data-only). The TubeMap graph *fan-out* is the
  visual; the *manifest branch* is the lineage split — Eve and I were using "branch" for different things.

### Why this design (false starts)

- I initially read "branch" as graph fan-out, then as "duplicate whole manifest." Both wrong. Eve's use-cases
  (different plots same data; wide-vs-long) are *in-manifest* bifurcations served by the existing 3-tier model —
  the "separate files" she meant are the fragment-per-component `!include` files, not duplicate manifests.
  Reading `config/manifests/pipelines/1_test_data_ST22_dummy.yaml` and its fragment tree resolved it.
- Did NOT mark BP-FORMS-1 done blindly — verified headless first, then split remaining work into precise tasks.

### Files touched

- `.claude/knowledge/architecture_decisions.md` — authored **ADR-082** (4-layer lock, 7 decisions, §2a refinements, MVP inventory, T1/T2-vs-T3 boundary, form-unification mandate, implementation order).
- `.claude/design/spaces/BLUEPRINT.md` — refreshed stale Current State; Branch row + Open-Questions resolution banner; status → "Build (locked by ADR-082)".
- `.claude/design/blueprint_full_feature_set_research.md` — status RESOLVED + pointer to ADR-082.
- `.claude/rules/rules_ui_dashboard.md §7` — L1–L4 layer table + branching-as-bifurcation definition.
- `.claude/tasks/tasks.md` — BP-FORMS-1 `[x]` (verified); BP-ESCAPE/UNDO/HELP `[x]` (implemented); BP-ADR-FULL-1 resolved; 13 Phase 32 (cont.) tasks added (BP-FORMS-UNIFY-1, BP-ENUM-PREVIEW-1, BP-EXPR-EDITOR-1, BP-COMPONENT-SCHEMA-1, BP-COMPONENT-FORMS-1, BP-JOINT-1, BP-GROUPS-1, BP-META-1, BP-NEW-1, BP-VALIDATE-1, BP-CSS-LEGEND-1, BP-SMOKE-1, BP-AUTOSAVE-1, BP-BRANCH-NODE-1); session note added.
- `tmpAI/verify_bp_forms.py` — headless 60-action form verification (agent scratch).

### @deps

- No `.py` / manifest interface changes. Touched `.claude/` md files only. Ran `build_dep_graph.py` →
  131 nodes, 256 edges; `dependency_index.md` regenerated. ✅

### Next

- No code written this session (design lock only). Implementation order locked in ADR-082 §Implementation Order:
  BP-FORMS-UNIFY-1 first, then the two ADR-075 widget gaps, then component schema→forms, Joint Designer, etc.
- Open implementation-level UX detail for BP-BRANCH-NODE-1: auto-detect bifurcation node vs. explicit user choice.

### Post-review revisions (same session, after Eve reviewed ADR-082)

- **Q4 revised** — was "comments lost on round-trip (documented limitation)." Verified the ingest already strips `#` comments (PyYAML `SafeLoader` + `yaml.dump`; ruamel not installed). Decision: intent documentation = free-text **`comment:` data field per node/step/group/plot** (preserved by the ingest, no new dep) + good-practice hover tooltip. `#` comments stay unpreserved (ruamel = v2). New task **BP-COMMENTS-1**.
- **Q6 confirmed** — sidebar inventory now; TubeMap context menu later is additive **iff** BP-GROUPS-1 builds the group/plot CRUD decoupled from the sidebar UI (added as a task constraint). No heavy refactor.
- **Save/Export** — confirmed both DONE (`btn_save_internal`, `btn_download_manifest`); BP-SMOKE-1 extended with a "BLUEPRINT-saved manifest loads cleanly in HOME" round-trip check.
- **Fixed ADR-082 internal inconsistencies** from the iterative Q3 re-lock (inventory row 10, Tasks bullet, Implementation Order v2 list — all still said "variants block").

## Session 2 — BP-FORMS-UNIFY-1 (implementation)

First implementation task off the ADR-082 roadmap. Goal: add-node uses the same `ui_schema` rich form as edit-node (gap #1, ADR-082 §5).

### What changed (`app/modules/wrangle_studio.py` only)

- **`add_node`** rewritten: non-join add now creates a stub node `{action, params:{}, comment:""}`, appends it, and sets `selected_node_idx` so the existing rich form (`bp_action_form_ui`, right sidebar) opens for configuration. User fills + Apply (`btn_bp_apply_node`, unchanged) commits. Join keeps its dedicated modal until BP-JOINT-1.
- **Retired primitive path:** removed `_finalize_add_node`, `show_annotation_modal`, the `handle_confirm_node` effect, the `pending_node` reactive value, and the `new_param_value` input.
- **Focus-tab UI:** moved `column_selector` into the join-only `panel_conditional` (relabelled "Left join key"); removed the primitive target/param fields; added a helper note directing non-join config to the right-sidebar form.
- `@deps` doc line gained ADR-082.

### Why this approach

The edit machinery (`_render_action_form` + `_bp_apply_node_handler`) already handles all 8 widget types and writes the selected node by index. Unification therefore needed no new form code — add just creates+selects a stub and reuses the verified edit path. Minimal surface, no duplication.

### Verification

- App imports clean; no dangling references to removed symbols.
- Headless: all 60 action forms render with empty (stub) params, zero failures (`tmpAI/verify_bp_forms.py`).
- Playwright smoke: 12 passed / 3 skipped / 2 failed. The 2 failures (`TestFilterPipeline` `#fb_op`) are **pre-existing full-suite flakiness** — proven by stash test: both pass in isolation WITHOUT my change (12.7s) and WITH my change (12.5s); only fail under full-suite load. My change touches BLUEPRINT only, not the HOME filter form.
- **Not done:** live browser confirmation of click-Add→form→Apply (no UI access; existing smoke suite has no BLUEPRINT coverage). Delegated to BP-SMOKE-1 (d).

### @deps

- Ran `build_dep_graph.py` ✅. `wrangle_studio.py` @deps `provides` unchanged (removed methods were never in `provides`); added ADR-082 to `doc:`.

## Session 3
Done: BP-ENUM-PREVIEW-1, BP-EXPR-EDITOR-1.

## BP-PLOT-MODEL-1 — 2026-05-20 (sonnet/high)

**Status:** DONE

**Changes:**
- `libs/viz_factory/src/viz_factory/plot_config_resolver.py`:
  - `_normalise_spec` promoted to public `normalise_plot_spec` with `_meta` passthrough (unknown/taxonomy keys collected into `_meta` dict, preserved verbatim through round-trip)
  - Added `_CANONICAL_SPEC_KEYS` frozenset constant
  - Added `serialise_plot_spec(canonical) -> dict` — inverse function: emits `mapping:` + explicit `layers:`; never `factory_id`; re-expands `_meta` to top level; strips internal `_tier` markers
  - Updated `resolve_plot_config` internal call from `_normalise_spec` → `normalise_plot_spec`
  - Updated `@deps` block (provides + consumed_by)
- `libs/viz_factory/src/viz_factory/viz_factory.py`:
  - Deleted dead `_standardize_config` (uncalled since VIZFAC-RENDER-WIRE-1)
  - Deleted dead `_auto_adjust_axis_labels` (uncalled since VIZFAC-RENDER-WIRE-1)
  - Updated `@deps` with removal note
- `libs/viz_factory/tests/test_plot_config_resolver.py`:
  - Updated import and all calls from `_normalise_spec` → `normalise_plot_spec`
- `libs/viz_factory/tests/test_plot_spec_roundtrip.py` (NEW):
  - 30 tests: `_meta` passthrough, idempotency, `serialise_plot_spec` correctness, round-trip render-equivalence, `_CANONICAL_SPEC_KEYS` completeness

**Results:** 103/103 tests pass in resolver + roundtrip suite; 175/175 in broader baseline. Zero regressions.

**Deps verified:** `build_dep_graph.py` run, `dependency_index.md` updated.

**Next:** BP-PLOT-LOAD-1 `[sonnet/high]` — requires model switch if still on sonnet.

---

## Session — BP-PLOT-LOAD-1 (Plot Spec → logic_stack population)

**Task:** BP-PLOT-LOAD-1 `[sonnet/high]` — BLUEPRINT `plot_spec` branch populates `logic_stack` with component nodes via `normalise_plot_spec`. Read-only (no commit — BP-PLOT-COMMIT-1 handles that).

**Files modified:**
- `app/handlers/blueprint_handlers.py` — `plot_spec` branch in `_do_load_component` (~line 282): calls `normalise_plot_spec(file_content)`, builds `plot_nodes` list (`{"component": "__mapping__", ...}` + `{"component": layer_name, ...}` per layer), sets `wrangle_studio.logic_stack`.
- `app/modules/wrangle_studio.py` — `logic_stack_ui`: now discriminates `component` vs `action` nodes; renders "aes" badge (teal) for `__mapping__`, "geom" badge (blue) for component layers, displays `display_name` instead of raw action key.
- `app/modules/wrangle_studio.py` — `bp_action_form_ui`: detects `component` key; returns read-only panel (aes pairs for `__mapping__`; params preview for geom layers; "read-only" badge + note); action nodes follow existing path unchanged.
- `@deps` updated in both files.

**Verification:**
- `from app.src.main import app` → import OK (no startup errors).
- `test_plot_spec_roundtrip.py` + `test_plot_config_resolver.py`: 103/103 pass.
- Baseline (`test_filter_operators.py` + `connector` + `test_deco2_components.py`): 97/97 pass.
- `build_dep_graph.py` run, `dependency_index.md` updated.

**Next:** BP-COMPONENT-FORMS-1 `[opus/high]` — requires model switch to opus.

---

## Session — BP-COMPONENT-FORMS-1 (Component form path) [opus/high]

**Task:** Route plot-layer (component) nodes through the same `ui_schema` rich-form path as wrangling action nodes. Depends BP-PLOT-MODEL-1 + BP-PLOT-LOAD-1 + BP-COMPONENT-SCHEMA-1 (all done).

**Files modified:**
- `libs/blueprint_arch/src/blueprint_arch/schema_registry.py` — added `search_components(query)` (parallel to `search_actions`, searches name/label/category/tags); `@deps` provides updated.
- `app/modules/wrangle_studio.py`:
  - `bp_action_context` selector: added `"plot": "Plot — Layers"` option.
  - `_update_action_picker`: branches on `ctx == "plot"` → populates `action_selector` from `get_components_for_context("plot")` + `search_components`, grouped by component `category` (11 categories: distribution, scale, theme, annotation, etc.).
  - `add_node`: decides node kind by `get_component_catalog()` membership → stub `{component: name, ...}` vs `{action: name, ...}` (names never collide).
  - `bp_action_form_ui`: component branch now renders editable form via `get_component_catalog()` + shared `_render_action_form` (comment field + Apply button + "geom" badge). `__mapping__` stays read-only (deferred to BP-MAPPING-FORM-1).
  - `_render_action_form` `number` widget: robustness fix — `float(default)` crashed when `default` was explicit `null`; now renders empty field (`value=None`).
  - `@deps` consumes updated.
- `app/handlers/blueprint_handlers.py` `_bp_apply_node_handler`: kind-branch — `node_kind_key`/`node_name` resolve from component vs action catalog; rebuilds node preserving the discriminator key; component edits set `invalidated_from=None` (plot layers don't propagate schema); `__mapping__` guarded out (defensive — has no Apply button); `@deps` consumes updated.

**Bug found + fixed:** `stat_ecdf.n` has `default: null` in its ui_schema → `_render_action_form` number widget did `float(None)` → TypeError. This was a latent bug in the shared form renderer (would hit any action/component with an explicit-null number default); surfaced now because components route through it. Fixed to render an empty numeric field. Apply handler already omits empty-number params, so an unset optional `n` is correct.

**Verification:**
- `from app.src.main import app` → import OK.
- Headless form render: 191/191 component forms + 60/60 action forms render, 0 failures (with `_bootloader=None` exercising the guarded color-palette path).
- `search_components('violin')` → geom_violin, geom_sina, stat_ydensity, stat_sina, position_dodge2 (sensible).
- Unit suites: 200/200 pass (filter_operators, connector, deco2_components, plot_spec_roundtrip, plot_config_resolver).
- Playwright smoke (qa persona): 14 passed, 3 skipped.
- `build_dep_graph.py` run; `dependency_index.md` updated.

**Note for next task:** `__mapping__` node is still read-only — editable aes form is BP-MAPPING-FORM-1 `[sonnet/medium]` (next). Component Apply path is live and exercised by the same `btn_bp_apply_node` handler. Live browser click-through (Add plot layer → form → Apply) folds into BP-SMOKE-1.

**Next:** BP-MAPPING-FORM-1 `[sonnet/medium]` (requires model switch back to sonnet) or BP-PLOT-COMMIT-1 `[opus/high]`.

---

## Session — BP-MAPPING-FORM-1 (Aesthetic mapping editable form) [sonnet/medium]

**Task:** Promote the `__mapping__` node from read-only to a full editable form with column pickers for all standard aes keys.

**Files modified:**
- `app/modules/wrangle_studio.py` — `bp_action_form_ui`: replaced `__mapping__` read-only block with editable form. 8 aes keys (`x`, `y`, `fill`, `color`, `size`, `alpha`, `shape`, `facet_by`) each get an `ui.input_select` with choices from `_extract_upstream_cols()` + sentinel `"(not set)"`. Input IDs: `bp_map_{key}` — distinct from `bp_form_*` (component/action widget namespace) to avoid collisions. Comment field + Apply button. Badge changes from grey "read-only" to teal "aes". @deps updated.
- `app/handlers/blueprint_handlers.py` — `_bp_apply_node_handler`: removed `__mapping__` early-return guard. Added dedicated `__mapping__` read path before the generic component/action branch: reads `bp_map_{key}` inputs, filters empty values, rebuilds `{"component": "__mapping__", "params": {...}, "comment": ...}`, sets `invalidated_from=None` (mapping edits don't propagate upstream schema). @deps updated.

**Why `bp_map_*` IDs:** The action/component form widgets use `bp_form_{param_key}`. The mapping form uses `bp_map_{aes_key}`. This separation means both can coexist in the DOM without ID collision if a future layout renders them together, and makes the Apply handler branch unambiguous.

**Verification:**
- `from app.src.main import app` → import OK.
- `tmpAI/verify_bp_mapping_form.py`: 5/5 tests pass (`_extract_upstream_cols`, col_choices construction, selected-value resolution, Apply handler logic, empty upstream).
- Baseline: 200/200 unit tests pass (filter_operators, connector, deco2_components, plot_config_resolver, plot_spec_roundtrip).
- `build_dep_graph.py` run; `dependency_index.md` updated.

**Next:** BP-PLOT-COMMIT-1 `[opus/high]` — requires model switch to opus.

---

## Session — BP-PLOT-COMMIT-1 (Correct commit + kill tier3 bug) [opus/high]

**Task:** ADR-083 §8. Make BLUEPRINT commit write the canonical model back to the correct file/tier, and remove the legacy tier3 dump.

**The pre-existing bug fixed:** `_handle_manifest_save_internal` and `btn_download_manifest` both wiped `tier1`/`tier2` and dumped the entire logic stack into `wrangling.tier3` of the **master manifest** — wrong tier (T3 is HOME-only, ADR-082 §4) *and* wrong file (master, not the loaded fragment).

**Scope decisions (with Eve, this session):**
- **Wrangling commit = full (tier-tag).** Eve chose the faithful design over the "plot-only descope" I floated. Each wrangling node carries a hidden `_tier` marker (source tier); commit regroups into `wrangling.{tier1,tier2}`. New nodes take their tier from the picker context selector (T1→tier1, T2→tier2). BLUEPRINT never writes T3.
- **Download = full multi-file manifest.** Eve: "download the full manifest with all dependencies." Implemented as a zip bundle: master `.yaml` + mirrored `basename/` tree (input_fields/, wrangling/, output_fields/, assembly/, plots/). Reflects on-disk state → workflow is Save → Download.
- **Draft autosave for unfinished work** = BP-AUTOSAVE-1 (separate, already on roadmap) — Eve raised this; confirmed not bundled here.

**Files modified:**
- `app/modules/wrangle_studio.py`:
  - Added `active_component_path` reactive value (loaded fragment file path; Save target). Empty for inline components.
  - `add_node`: new action stubs get `_tier` from `bp_action_context` (t2→tier2, else tier1). Component stubs unchanged.
  - `@deps` updated.
- `app/handlers/blueprint_handlers.py`:
  - Import: added `serialise_plot_spec`; added `import zipfile`.
  - `_parse_logic_to_nodes`: tags each node with `_tier` (tier1/tier2; legacy tier3 folded → tier2).
  - `_do_load_component`: sets `active_component_path` (Mode A → `str(abs_file)`; Mode B inline → `""`).
  - `_bp_apply_node_handler`: preserves `_tier` across edits (action nodes only).
  - New pure helper `_serialise_component_for_save(role, nodes, raw_text)`: plot_spec → `serialise_plot_spec` (mapping+layers from live stack, scalars/taxonomy recovered from original via `normalise_plot_spec`, `spec:` wrapper preserved); wrangling/plot_wrangling → `{wrangling|recipe: {tier1, tier2}}` routed by `_tier`, `comment` preserved, `_tier` stripped.
  - `_handle_manifest_save_internal`: rewritten to write `active_component_path` via the helper; warns for inline/missing; no tier3 dump.
  - `btn_download_manifest`: rewritten to emit the full multi-file zip bundle (master + mirror tree); filename `<stem>_bundle_<ts>.zip`.
  - `@deps` updated.

**Verification:**
- `from app.src.main import app` → import OK.
- `tmpAI/verify_bp_plot_commit.py`: 5/5 — bare round-trip, spec-wrapped round-trip (target_dataset/theme/taxonomy preserved), legacy factory_id→geom_bar (no factory_id/flat-aes re-emitted), wrangling tier routing (_tier strip + comment), tier3 fold-to-tier1.
- Download bundle verified against real `2_test_data_ST22_dummy` tree → 12 entries (master + assembly/input_fields/plots/wrangling fragments).
- `tmpAI/verify_bp_mapping_form.py`: 5/5 (no regression).
- Baseline: 200/200 unit tests pass. Playwright smoke (qa): 14 passed / 3 skipped.
- `build_dep_graph.py` run; `dependency_index.md` updated.

**Notes for next tasks:**
- Live browser click-through (load plot → edit mapping/layer → Save → reload → Download bundle) folds into BP-SMOKE-1 (no BLUEPRINT coverage in the current smoke suite).
- `__mapping__` node with empty mapping now serialises to `mapping: {}` (omitted by serialise_plot_spec when falsy) — acceptable; a plot with no aes is author error caught downstream.
- The transitional `normalise_plot_spec` legacy reader is still active (factory_id/flat-aes expand). Removal is gated on BP-PLOT-MIGRATE-1 + BP-PLOT-DOCS-1 → BP-PLOT-LEGACY-REMOVE-1.

**Next:** BP-PLOT-MIGRATE-1 `[sonnet/medium]` (manifests + generators) or BP-PLOT-DOCS-1 `[sonnet/medium]` (format authority) — both `[sonnet]`, require model switch. Then BP-PLOT-LEGACY-REMOVE-1 `[sonnet/high]`.

---

## BP-PLOT-MIGRATE-1 [sonnet/medium] — 2026-05-20

**Task:** Migrate 19 legacy plot specs + master manifest inline specs to canonical (no factory_id, explicit geom layers, mapping: block).

**Files created:**
- `assets/scripts/migrate_plot_specs.py` — argparse migration script with `_IncludeLoader`/`_IncludeDumper` YAML Loader/Dumper subclasses that preserve `!include` directives on round-trip (mirrors `persona_validator.py` pattern).

**Files migrated (21 total):**
- 15 standalone plot spec fragments in `config/manifests/pipelines/*/plots/`
- 3 figshare_integration plot specs
- 1 `1_Abromics_general_pipeline.yaml` (master — 2 inline plot specs)
- 1 `demo_abromics.yaml` (master — 1 inline)
- 1 `stress_test_master.yaml` (master — 1 inline, plus `!include` preserved)
- 2 templates: `audit.yaml` (2 inline), `e_coli.yaml` (2 inline), `simple_project_template.yaml` (1 inline)
- `create_manifest.py:281` generator patched to emit canonical `{mapping, layers: [geom_bar]}` scaffold

**Migration rules applied:**
- `factory_id: bar_logic` + no `y` → `geom_bar` prepended
- `factory_id: bar_logic` + `y` in mapping → `geom_col` prepended
- `factory_id: heatmap_logic` → `geom_tile` + `color` remapped to `fill` in mapping
- `factory_id: boxplot_logic` → `geom_boxplot`
- `factory_id: violin_logic` → `geom_violin`
- Flat aes keys (`x`, `y`, `fill`, `color`, ...) → `mapping:` block (removed from top level)

**Verification:**
- `grep -rn "factory_id" config/manifests/` → 0 hits
- `normalise_plot_spec` roundtrip on 4 representative files → all produce correct canonical
- `!include` directives in master manifests preserved correctly (grep confirms)
- 125/125 baseline tests pass (`test_filter_operators`, `connector`, `deco2_components`, `test_plot_spec_roundtrip`)
- App import OK

**Next:** BP-PLOT-DOCS-1 `[sonnet/medium]` — remove factory_id from format-authority docs.

---

## BP-PLOT-DOCS-1 — 2026-05-20

**Task:** Remove `factory_id` from all format-authority documentation.

**Files updated:**
- `.claude/rules/rules_manifest_structure.md §8`: removed `factory_id` table + flat-aes example; replaced plot spec example with canonical grammar-of-graphics format (mapping: block + explicit geom layer); updated geom list note.
- `.claude/rules/rules_persona_bioscientist.md §3-C`: replaced factory_id example with canonical mapping: + geom_bar layer format; added tombstone note that engine raises PipelineError on factory_id.
- `docs/appendix/manifest_structure.yaml`: replaced factory_id in plot_spec_file_format with canonical format + tombstone comment.
- `.claude/knowledge/manifest_data_contract_rules.md`: 3 instances (§5.3 downstream example, §8.1 spec wrapper example, pipeline diagram) all updated to canonical format.
- `.claude/rules/rules_viz_factory.md §7`: cascade constraint: `factory_id` → `layers` (factory_id no longer a valid key).

**Confirmed already canonical (no changes needed):**
- `.claude/rules/rules_gallery_standards.md` — uses mapping: + name: layers throughout.
- `.claude/knowledge/project_conventions.md` — no factory_id present.
- `assets/template_manifests/1_test_data_ST22_dummy.yaml` — no factory_id present.

**Status:** Only tombstone "not supported" warnings (3 files) and ADR-083 historical text remain. All authoring examples now canonical.

**Next:** BP-PLOT-LEGACY-REMOVE-1 `[sonnet/high]` — hard-error in normalise_plot_spec for factory_id/flat-aes.
