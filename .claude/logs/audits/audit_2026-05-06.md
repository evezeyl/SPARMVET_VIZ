Status: PROCESSED 2026-05-09 — pre-protocol session log; findings absorbed into current codebase and tasks.md
# Session Audit — 2026-05-06

## Session goal

Repo hygiene: fix broken `rules_persona_feature_flags.md`, rewrite `.gitignore` / `.aiignore`, separate `EVE_WORK/` into an independent sibling git repo, move `presentation/` there, and clean SPARMVET_VIZ's tracked-file set.

---

## Changes Made

### Wave 1 — rules_persona_feature_flags.md surgical repair (commit in prior session, carried over)

- **Group A table**: repaired broken `export_enabled` row — description had detached from the flag cell after bulk regex pass.
- **Group B cascade**: removed `└─ export_enabled` entry; added explicit clarification paragraph: "`export_enabled` is NOT in this group — export is independent of interactivity."
- **Flag matrix**: collapsed two duplicate `export_enabled` rows into one; added `blueprint_enabled` and `test_lab_enabled` rows.
- **Cascade Enforcement §1**: removed `Force export_enabled = False` line; added note that export is NOT forced by `interactivity_enabled=False`.

### Wave 2 — .gitignore and .aiignore rewrite (commit `ccba07f`)

**`.gitignore`** additions over previous version:
- `tree.txt`, `manifest.json` (generated/stale snapshots)
- `.claude/conversations/` (ephemeral session state)
- `.claude/logs/sessions/`, `.claude/logs/handoffs/archive/`
- `.vscode/bookmarks.json`, `.vscode/favorites/`, `.vscode/settings.json`
- `.directory`, `*.ods`, `Thumbs.db`
- Cleaned up legacy `.antigravity/` references (now under `.claude/`)

**`.aiignore`** additions / improvements:
- Added `*.png`, `*.jpg`, `*.jpeg`, `*.svg`, `*.pdf` (binary/image files add no AI context)
- Added `*.ods`, `.claude/conversations/`, `presentation/`
- Removed all dead `.antigravity/` and `.agents/` path references
- Added section comments for clarity

### Wave 3 — EVE_WORK sibling repo

- `git init` in `../EVE_WORK/` — independent local-only git (no GitHub remote needed; backed up to external disc)
- Initial commit: 228 files (all original EVE_WORK content preserved)
- `presentation/` moved (`mv presentation/ ../EVE_WORK/presentation/`) and committed in EVE_WORK (`675700a`)
- `SPARMVET_VIZ.code-workspace`: updated `🍊 EVE` folder path from `"EVE_WORK"` (inside repo) → `"../EVE_WORK"` (sibling) — VSCode multi-root workspace remains intact

### Wave 4 — SPARMVET_VIZ index cleanup

- `git rm -r presentation/` — 11 files removed from index (commit `a747839`)
- Working tree clean; 4 commits ahead of `origin/dev` (not yet pushed)

### Tasks logged

- **REPO-CLEAN-1** `[haiku/low]` `[soon — after backup]`: full `git filter-repo` history purge with exact commands in `tasks.md`. Gated on: external disc backup confirmed + gdrive sync complete.
- **ADR-011 cross-lib violations** `[opus/high]`: catalogued 5 sites (blueprint_arch, transformer, viz_factory → utils; transformer → ingestion). Pragmatic note: `utils` has no upstream lib deps so may be treated as a "base" lib rather than peer. Invasive refactor deferred.
- **UTILS-RELOC-2** `[haiku/low]`: `gallery_manager.py` canonical copy decision (utils vs viz_gallery) — awaiting user input.
- **In-app contextual help** `[sonnet/high]`: 3 design options logged; decision gate: air-gapped vs. internet deployment context must be confirmed first.

### New library READMEs (Wave 2, carried from prior session)

- `libs/blueprint_arch/README.md` — ManifestNavigator, BlueprintMapper, design constraints (zero Shiny, read-only), install, tests.
- `libs/test_lab/README.md` — AquaSynthesizer, ManifestBootstrapper, XlsxExtractor, KeyReconciler, CLI usage.

---

## Test results

- No Python code changed this session — no test runs required.

---

## What was NOT done / deferred

- `git push origin dev` — 4 commits pending push. User to run when ready.
- Playwright smoke suite — not re-run (no Shiny reactive code changed).
- Transformer README audit — noted but not critical.
- REPO-CLEAN-1 — intentionally deferred; requires backup confirmation first.

---

## False starts

- **Shell CWD drift into EVE_WORK**: after `cd ../EVE_WORK && git init && git commit`, the Bash tool's working directory stayed in EVE_WORK. Caused `git rm --cached presentation/` to run in the wrong repo (EVE_WORK had no presentation/ in its index yet). Discovered when `ls .gitignore` returned "No such file or directory". Fixed by explicit `cd /home/evezeyl/.../SPARMVET_VIZ` prefix on all subsequent commands. The `presentation/` index removal in SPARMVET_VIZ had to be repeated in the next context as the first action.

---

## @deps update

No Python or manifest files modified. No `@deps` block updates required this session.
