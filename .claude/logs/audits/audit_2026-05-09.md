# Session Audit — 2026-05-09

## Session goal

Design decisions sprint: close pending REVIEW items, decide ADR-073 (Configurable Sidebar Slot Registry), propagate all decisions to rules, workflows, knowledge, docs, tasks, implementation plan.

---

## Changes Made

### Wave 1 — REVIEW items closed (no code changes)

- **REVIEW-SCOPING-1**: Confirmed already implemented (PersonaValidator Rule 6). Closed. ✅
- **REVIEW-EXPORT-FLAGS**: One `export_enabled` flag is sufficient — 3-way scope toggle already covers global/group/plot. No separate `export_group_enabled` flag. Closed. ✅
- **REVIEW-AUTOSAVE-CACHE**: Confirmed already implemented (SESSION-PERSONA-1). Parquet cache always written; ghost_save gated. Closed. ✅
- **REVIEW-HASH-EXPORT**: Always include all hashes in every export, no flag. Per-source-file SHA256 table required (not just rolled-up `data_batch_hash`). ADR-069 amended. Closed. ✅

### Wave 2 — Decisions made

- **UTILS-RELOC-2**: `libs/viz_gallery/` is the canonical copy of `gallery_manager.py`. Delete `libs/utils/` copy and fix imports.
- **STATIC-VIEW-1c**: Left sidebar visibility is persona-template controlled (`workspaces.home.left_sidebar.visible`). Static personas show `project_info` + `deployment_info` + `export`. Hiding allowed when `export_enabled: false`. Resolved by ADR-073.
- **REVIEW-EXPORT-FLAGS**: Confirmed: bundle must include per-source-file hash table (individual SHA256 per dataset, not just `data_batch_hash` rollup). ADR-069 amended with this requirement.

### Wave 3 — ADR-073: Configurable Sidebar Slot Registry

**Authored and decided. Implementation pending (Phase 31).**

Key decisions:
- **Two independent layers**: slot list (layout, in persona template) vs feature flag (capability, unchanged). A disabled flag silently skips a panel in the slot list.
- **Per-workspace configuration**: `workspaces.home`, `workspaces.blueprint`, `workspaces.gallery`, `workspaces.test_lab` — each with independent `left_sidebar` and `right_sidebar`.
- **`!include` shared configs**: `config/ui/sidebars/` holds reusable panel-list YAML files. Multiple personas sharing the same layout reference the same file.
- **`PANEL_REGISTRY`** in `app/modules/sidebar_registry.py` — headless-safe (Two-Category Law). Each entry: panel type name → renderer ref + gate flag.
- **Visibility control**: `visible: true/false` in template. Auto-hide if `visible` unset and all panels skip.
- **Fixes ADR-053 violation (task 25-O)**: right sidebar structural exclusion moves from persona-name string comparison in `ui.py` → `bootloader.get_sidebar_config("home", "right").visible`.
- **`SidebarValidator`** + `scripts/validate_persona_config.py --all --strict` for CI and pre-deployment gate.

### Wave 4 — Ecosystem propagation

All affected files updated to reflect ADR-073 and the closed decisions:

| File | Change |
|---|---|
| `.claude/knowledge/architecture_decisions.md` | ADR-073 added (full spec); ADR-069 amended (per-source-file hash table) |
| `.claude/tasks/tasks.md` | 4 REVIEW items closed; STATIC-VIEW-1c closed; SIDEBAR-CONFIGS-1, SIDEBAR-REGISTRY-1, SIDEBAR-VALIDATE-1 added |
| `.claude/plans/implementation_plan_master.md` | Phase 31 added (SIDEBAR + EXPORT provenance cluster) |
| `.claude/rules/ui_implementation_contract.md` | §11 replaced (Phase 25-E hardcoded spec → ADR-073 slot registry spec) |
| `.claude/rules/rules_ui_dashboard.md` | §2 rewritten to reference ADR-073; right sidebar layout note updated |
| `.claude/rules/rules_persona_feature_flags.md` | Right sidebar note updated to reference ADR-073 / bootloader API |
| `.claude/knowledge/project_conventions.md` | §3 rewritten for ADR-073; file registry rows added for `sidebar_registry.py`, `config/ui/sidebars/`, `validate_persona_config.py` |
| `docs/workflows/ui_persona.qmd` | New "Sidebar Configuration (ADR-073)" section added |
| `README.md` | Sidebar configurability + `validate_persona_config.py` mentioned in deployment section |

---

## Test results

No code changed this session — design and documentation only. No tests required.

---

### Wave 5 — Remaining review items from session end

Additional decisions made after Wave 4:

| Item | Decision | Task |
|---|---|---|
| **PREVIEW-ALLROWS-1** | "Show all rows" toggle in data preview — off by default, hover warning, respects tier toggle | Task added to `tasks.md` |
| **RESEARCH-LIMS-1** | Audit database / LIMS integration — deferred until pilot funding and concrete LIMS target. ADR decision deferred. | Task added `[deferred]` |
| **In-app contextual help** | Air-gapped first. Two-tier: contextual modal cards (`app/src/help/`) + optional `docs/_site/` served at `/docs/`. | HELP-INLINE-1 + HELP-DOCS-1 tasks added |
| **DEPLOY-CONNECT-1** | Posit Connect library install: add `-e ./libs/xxx` relative entries to `requirements.txt`. Entry point: `app/src/main.py`. Connect profile template. Clean env smoke test. Pre-existing done: CDN vendoring, nav gating, vendor manifest, install script. | Task + Phase 31-I added |

---

## What was NOT done / deferred

- No implementation of ADR-073 (Phase 31) — design only.
- `UTILS-RELOC-2` (delete `libs/utils/gallery_manager.py` and fix imports) — decided but not implemented.
- Export provenance cluster (EXPORT-AUDIT-COMPLETE-1, EXPORT-HASH-2, EXPORT-VERSION-1, EXPORT-IMG-META-1) — tasks exist, not started.
- `UI-TITLE-1`, `IMPORT-UI-1` — decided earlier, implementation pending.
- `DEPLOY-CONNECT-1` — decided, task registered, not implemented.

---

## False starts

None — session was design and documentation only.

---

## @deps update

No Python or manifest files modified. No `@deps` block updates required this session.
