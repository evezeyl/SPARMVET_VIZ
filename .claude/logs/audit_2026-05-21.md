Status: PROCESSED 2026-05-21 — continuation session; two tasks completed.

# Session — BP-JOINT-1 follow-up + YAML Boolean Trap prevention

**Date:** 2026-05-21 (continuation)
**Tasks completed:** YAML boolean trap documentation + ConfigManager prevention; Playwright smoke test fix.

## Audit triage (session start)
Prior session log (`audit_2026-05-21.md` — BP-JOINT-1) already processed. No unprocessed reports.

## Task 1 — Playwright smoke test fix (test_add_filter_row / test_filter_reset_clears_rows)

Both tests were flaky in the full suite due to a race condition: `filter_form_ui` re-renders
after `active_home_subtab` changes, but the column options come from a LazyFrame scan of the
target dataset parquet. On first navigation, the parquet may not yet exist and the form shows
stale columns from the prior dataset.

**Fix:** Added `_wait_for_fb_col_option(page, "year", timeout=20_000)` helper to
`app/tests/test_shiny_smoke.py`. The helper polls `#fb_col option` via `wait_for_function`
before calling `select_option("year")`. Applied to both `test_add_filter_row` and
`test_filter_reset_clears_rows`.

**Result:** 137 passed, 3 skipped (expected persona skips). All clean.

## Task 2 — ConfigManager-level YAML boolean trap prevention

**Background:** `DataAssembler` has a `step.get(True)` fallback for the unquoted `on:` boolean
trap, but ConfigManager had no prevention — it loaded `True` as a dict key and passed it downstream.

**Fix:** Added `_normalise_yaml_boolean_keys(config, manifest_path)` module-level helper to
`libs/utils/src/utils/config_loader.py`. Called on `self.raw_config` immediately after
`yaml.load()` completes (after `!include` constructors have merged all fragments). The function:
- Recursively walks the entire config dict/list tree
- Converts any `True` dict key to the string `"on"` in place
- Prints a warning naming the manifest file and referencing rules_manifest_structure.md §7

**Verified:** No false positives on the test manifest (all `'on':` are already properly quoted).
Boolean True → string "on" conversion confirmed via unit test.

**Files modified:**
- `libs/utils/src/utils/config_loader.py` — new `_normalise_yaml_boolean_keys` helper + call site + @deps update
- `app/tests/test_shiny_smoke.py` — new `_wait_for_fb_col_option` helper + usage in two test methods

## Task 3 — BP-COMMENTS-1: BLUEPRINT form comment tooltips + plot layer comment save fix

Added `ui.tooltip()` wrappers to all four `comment:` inputs in `wrangle_studio.py`:
- **Mapping form** (`bp_form_comment`): "Write your intent — why these aesthetics, not what the columns are."
- **Component/geom form** (`bp_form_comment`): "Write your intent — why this layer, not what it renders."
- **Wrangling action form** (`bp_form_comment`): "Write your intent — why this transformation, not what it does."
- **Joint Designer** (`joint_comment`): "Write your intent — why merge these datasets, not how the join works."

Fixed `_serialise_component_for_save` in `blueprint_handlers.py` (plot_spec role): `comment` key was
silently dropped when building layer dicts. Now preserved when non-empty.

**Files modified:**
- `app/modules/wrangle_studio.py` — 4 `ui.tooltip()` wrappers added
- `app/handlers/blueprint_handlers.py` — `_serialise_component_for_save` plot layer comment fix

## Rules/docs sync (prior session — verified current)
- `rules_manifest_structure.md §7` YAML Boolean Trap section expanded (done in prior session)
- `libs/blueprint_arch/src/blueprint_arch/join_designer.py` docstring updated (done in prior session)

## @deps
Updated `config_loader.py` @deps block: added `func:_normalise_yaml_boolean_keys` to provides,
added `rules_manifest_structure.md#7` to doc.

`build_dep_graph.py` to be run as session-end step.

---

# Session — BP-BRANCH-NODE-1: node-level lineage bifurcation (ADR-082 Q3)

**Date:** 2026-05-21 (continuation)
**Task completed:** BP-BRANCH-NODE-1 `[opus/high]` — replace the Visual Fork with node-level lineage bifurcation.

## Audit triage (session start)
`grep -rL "^Status: PROCESSED" .claude/logs/audits/*.md` → no unprocessed reports. Working tree clean at HEAD 62fda10.

## Design decisions (with Eve)
- **Bifurcation point = the selected TubeMap node** (no DAG auto-detect). Simplest, matches the existing fork selection model.
- **Divergent fragments seed empty** (no prefill). Eve's mental model: shared = source + input_fields (+ Tier 1 trunk), divergence = Tier 2 + output_fields. Matches canonical `Summary`/`Summary_quality` (empty wrangling, own output_fields). Avoids stale copies.
- **Plot branch is the exception** — copies the spec, because a plot is a leaf and an empty spec cannot render.
- **Deferred v2:** sharing the original's Tier 1 by sub-`!include` so a branch reuses the trunk's tier1 and only authors a new tier2 (Eve's "split & concatenate tiers" idea). Not needed for the shallow branching expected now.

## Files modified
| File | Change |
|---|---|
| `libs/blueprint_arch/src/blueprint_arch/manifest_navigator.py` | Replaced `generate_fork_yaml` with pure `generate_branch_plan(master_path, schema_id, role, new_id)`. Non-resolving loader preserves `!include` as strings; shares upstream by reference; writes new empty divergent fragments; text-level master insertion preserves all existing `!include` tags. Added `import re`; updated module docstring + @deps `provides:`. |
| `app/handlers/blueprint_handlers.py` | Reworked Visual Fork → Branch Node: `bp_branch_ui`, `bp_branch_preview_ui`, `_handle_branch_preview`, `_handle_branch_write`, `_write_branch_plan` (YAML-validate + no-clobber + rollback), `_clear_branch_preview_on_node_change`. Now works WITH `!include` manifests (old fork refused them). Import + @deps updated. |
| `app/handlers/home_theater.py` | Card "Fork Node" → "Branch Node"; mount `bp_fork_ui` → `bp_branch_ui`. |
| `.claude/knowledge/project_conventions.md` | Updated navigator function list + the `generate_branch_plan` reference row. |
| `.claude/plans/implementation_plan_master.md` | Visual Forking line → Branch (BP-BRANCH-NODE-1) supersession note. |
| `.claude/tasks/tasks.md` | BP-BRANCH-NODE-1 marked done with full notes. |

## Why the rework was needed
The legacy Visual Fork was doubly broken for real manifests: (1) `generate_fork_yaml` inlined **empty** `input_fields` instead of sharing the original's `!include` (no upstream sharing), and (2) `_write_fork_to_manifest` **refused** any manifest containing `!include` — i.e. every modular manifest. The branch model fixes both: shared upstream by reference, and text-level master insertion that leaves `!include` tags intact.

## Verification
- Headless `generate_branch_plan` (tmpAI scratch, since cleaned): data_schema / join / plot roles all `ok`; error cases (bad id, dup id, missing schema) correct; **full ConfigManager re-parse of a sandbox branch confirmed `Summary_v2.input_fields == Summary.input_fields`** (shared upstream by reference).
- `python -c "from app.src.server import server"` → OK.
- `build_dep_graph.py` → regenerated `dependency_index.md` + `tmp/dep_graph.json`.
- BLUEPRINT Playwright smoke (`SPARMVET_PERSONA=qa`) → **12/12 passed in 13.62s**.

## @deps
Updated in both `manifest_navigator.py` (provides `generate_branch_plan`) and `blueprint_handlers.py` (consumes `generate_branch_plan`; provides renamed branch outputs/effects). `build_dep_graph.py` run as session-end step.

## Environment note
Mid-session the approval/permission stream closed for ~all `./.venv/bin/python` and Edit calls ("Tool permission request failed: Error: Stream closed"). Eve reset the session bridge; tools recovered. All verification was run after recovery.
