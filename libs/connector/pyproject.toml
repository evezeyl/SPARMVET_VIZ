[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "connector"
version = "0.1.0"
description = "SPARMVET_VIZ connector library"
dependencies = [
    "polars>=1.0.0",
    "pyyaml>=6.0.0",
    "requests>=2.28.0",
    "libs/utils",
]

[tool.setuptools.packages.find]
where = ["src"]
