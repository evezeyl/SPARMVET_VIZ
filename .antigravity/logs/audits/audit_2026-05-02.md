# Audit Log — 2026-05-02

## Session — Pipeline persona bug fixes & CSS theme externalisation

**Branch:** dev
**Agent:** Claude Sonnet 4.6
**Commits today:** `3b0d093`, `788d6ff`

---

### Goals

1. Fix pipeline persona data-loading failures (pipeline-static, pipeline-exploration-simple) introduced in Phase 25.
2. Hide the column selector for non-interactive personas.
3. Extract CSS from Python into a proper stylesheet with per-persona override support (ADR-055).

---

### Batch 1 — Pipeline persona bug fixes (commit `3b0d093`)

Three bugs prevented pipeline personas from loading any data:

**Bug 1: `bootloader.get_default_project()` wrong project.**
Previously returned the first filesystem glob result. Fixed to check `fixed_manifest` from the persona template first.
File: `app/src/bootloader.py` — `get_default_project()`

**Bug 2: `server.active_cfg()` throws when manifest selector hidden.**
Was calling `input.project_id()` directly, which Shiny raises on when the input does not exist (hidden selector for pipeline-static / pipeline-exploration-simple). Fixed to use `_safe_input(input, "project_id", bootloader.get_default_project())`.
File: `app/src/server.py` — `active_cfg()` reactive calc

**Bug 3: Filters accordion shown for pipeline-static.**
`home_theater.py` was always rendering the Filters accordion. Fixed: gates the accordion on `bootloader.is_enabled("interactivity_enabled")`. When false, the accordion is absent entirely (not hidden inside — the panel itself is not created).
File: `app/handlers/home_theater.py`

---

### Batch 2 — Column selector hidden for non-interactive personas (commit `788d6ff`)

`home_col_selector_ui` now returns `ui.div()` early when `interactivity_enabled=False`. No "Visible columns (preview only)" or "Columns (drop unselected via audit)" control is shown.
File: `app/handlers/home_theater.py` — `home_col_selector_ui()`

Also fixed: radio button "Quality" label overlap with "Web / Presentation" choice — the aggressive `margin-bottom: 0px !important` on ALL `#nav_sidebar label` was killing spacing for radio groups. Added specific override rules for `.shiny-input-radiogroup .control-label` and `.form-check label`.

---

### Batch 3 — CSS theme externalisation (ADR-055, commit `788d6ff`)

**Before:** `CSS_THEME` was a ~150-line Python string embedded in `app/src/ui.py`.

**After:**
- `config/ui/theme.css` — new file, authoritative base stylesheet, 15 labelled sections. Fixed latent bug: a CSS comment using `#` (Python syntax) replaced with `/* */`.
- `app/src/ui.py` — reads the CSS file via `bootloader.get_theme_css_path()` at module load; injects via `ui.tags.style()`. `CSS_THEME` string removed.
- `app/src/bootloader.py` — new method `get_theme_css_path()`: reads `theme_css` key from persona config (falls back to `"config/ui/theme.css"`).
- All 6 persona templates (`config/ui/templates/*_template.yaml`) — each now has `theme_css: "config/ui/theme.css"` after the `logic_access` line.

**Effect:** Deployments brand the dashboard by creating a custom CSS file and pointing `theme_css:` at it. No Python changes needed.

---

### Architectural decision recorded

- **ADR-055** — CSS Theme Architecture — Externalised Stylesheet & Per-Persona Override. See `.antigravity/knowledge/architecture_decisions.md`.

---

### Files changed

| File | Change |
|---|---|
| `app/src/bootloader.py` | `get_default_project()` fix; new `get_theme_css_path()` method |
| `app/src/server.py` | `active_cfg()` — use `_safe_input` for `project_id` |
| `app/handlers/home_theater.py` | Filters accordion gated on `interactivity_enabled`; `home_col_selector_ui()` early-return; radio label CSS fix |
| `app/src/ui.py` | Load CSS from file via bootloader; remove `CSS_THEME` string |
| `config/ui/theme.css` | New file — extracted + corrected base stylesheet |
| `config/ui/templates/*_template.yaml` (×6) | Added `theme_css: "config/ui/theme.css"` |

---

### Known state after session

- Pipeline personas (`pipeline-static`, `pipeline-exploration-simple`) now load data correctly.
- Column selector absent for non-interactive personas.
- Filters accordion absent for pipeline-static (not just empty — panel not created).
- CSS styling is a proper CSS file, editable with CSS tooling and diffable in version control.
- No known regressions introduced.

---

## Session — UI Harmonisation Phase 26 (CSS + view refactors)

**Branch:** dev
**Agent:** Claude Sonnet 4.6

---

### Goals

1. Audit and fix all cross-view visual inconsistencies (Blueprint Architect, Test Lab, Gallery vs. Home baseline).
2. Establish view title banner pattern (`.view-title-banner`) across all non-Home views.
3. Refactor Gallery internal sidebar → persistent left `#nav_sidebar` (ADR-057).
4. Standardise all button colours to canonical colour system (blue `#345beb`, teal `#10a395`, amber `#ffc107`).
5. Fix sidebar collapse toggle cross-wiring bug.
6. Fix T3 propagation modal "Apply to:" radio spacing.

---

### CSS changes (`config/ui/theme.css`)

**Section 4 — Sidebar collapse toggle bug fix**
- Selector `#main_layout_inner .bslib-sidebar-layout > .collapse-toggle` was a descendant selector that matched nothing (since `#main_layout_inner` IS the `.bslib-sidebar-layout`), causing both toggles to land at the same position and cross-wire each other.
- Fixed: `#main_layout_inner > .collapse-toggle` (direct child combinator).

**Section 3 — Radio group spacing extended to modals**
- Existing `margin-top: 0 !important` override for stacked radio `.shiny-options-group` was scoped to `#nav_sidebar` only.
- Extended to `.modal` context — fixes "Apply to:" label-to-choices gap in T3 propagation modal.

**Section 6 — Button colour harmonisation**
- `#btn_upload_replace`, `#btn_upload_append`, `#btn_import_manifest` → teal (exchange/export category)
- `#btn_save_internal` → blue (primary action)
- `#btn_autofill_meta` → blue (action)
- `#btn_generate_data`, `#btn_apply_gallery_filters` → blue (primary actions); inline style overrides removed from Python
- `#btn_clear_stack`, `#btn_normalize_fields` → keep `btn-outline-danger` / `btn-warning` (destructive / warning semantics correct)

**Section 9 — Wrangle Studio typography + Blueprint tubemap accordion blue**
- `.wrangle-studio-container h4/h5/h6`, `label`, `.form-select/.form-control` — 3-tier scale applied
- `#blueprint_tubemap_accordion .accordion-button` → `#345beb` (consistent with Focus/Interface/YAML nav-pill tabs)

**Section 13 — Gallery sidebar typography**
- `.gallery-filter-title` scaled from `1.1rem/800` to `0.85rem/700 + uppercase` (Primary tier, matches sidebar section headings)

**New: `.view-title-banner` component (between sections 12 and 13)**
- `.view-title-banner` — rounded, shadowed banner container
- `.banner-title` — `1.0rem / 700`
- `.banner-subtitle` — `0.78rem / 400 / muted`

---

### Python changes

| File | Change |
|---|---|
| `app/modules/gallery_viewer.py` | New `build_sidebar_ui()` method — accordion with Recipe + Gallery Taxonomy panels. `render_explorer_ui()` refactored to full-width main content + banner (no `ui.layout_sidebar`). `@deps` updated. |
| `app/handlers/home_theater.py` | `sidebar_tools_ui` Gallery branch: calls `gallery_viewer.build_sidebar_ui()` (was "Discovery Mode Active" placeholder). `@deps` updated to list `gallery_viewer.py` as consumed. |
| `app/modules/dev_studio.py` | `h4("Developer Studio: …")` + `p(…)` + `hr` → `.view-title-banner` with "Test Lab: Synthetic Engine" (canonical name). |
| `app/modules/wrangle_studio.py` | `h4("Blueprint Architect Flight Deck")` → `.view-title-banner` with subtitle "Pipeline overview — helps you build manifests." |
| `app/handlers/gallery_handlers.py` | `_update_gallery_options`: "Visual Gallery (N matched)" label changed from `text-success` (Bootstrap green) to `style="color:#345beb"`. |

---

### Architectural decisions recorded

- **ADR-056** — View Title Banner Pattern & "Test Lab" Rename. See `architecture_decisions.md`.
- **ADR-057** — Gallery View Structural Refactor — Internal Sidebar → nav_sidebar Accordion. See `architecture_decisions.md`.

---

### Dependency index regenerated

`assets/scripts/build_dep_graph.py` re-run after `@deps` updates in `gallery_viewer.py` and `home_theater.py`.

---

### Known state after session

- All three non-Home views (Blueprint Architect, Test Lab, Gallery) now open with `.view-title-banner`.
- Gallery filters and recipe selector live in `#nav_sidebar` accordion; main content is full-width.
- "Developer Studio" label retired — "Test Lab" is canonical in all user-visible text. Module file stays `dev_studio.py`.
- Collapse toggles behave independently (left hides/shows left sidebar only, right hides/shows right sidebar only).
- T3 propagation modal "Apply to:" radio choices have correct spacing.
- Blueprint tubemap accordion header is canonical blue (`#345beb`), matching the Focus/Interface/YAML tab pills.
- All export/exchange buttons are teal; all primary-action buttons are blue; reset/discard buttons are amber.
- Deferred: Dev Inspector (Test Lab right sidebar) functionality — see tasks.md UX-DEVINSP-1.
- Deferred: Gallery Explorer (Gallery right sidebar) functionality — see tasks.md UX-GALLEXP-1.
- Deferred: Liberation Sans in test manifests — see tasks.md UX-FONT-1.

---

## Session — Phase 26 Final: Filter fixes, IMPORT-1, VizFactory integer breaks, export & docs

**Branch:** dev
**Agent:** Claude Sonnet 4.6

---

### Goals

1. Close remaining Phase 26 open items: full_recipe.yaml in SGE bundle, multi-file upload hint.
2. Implement IMPORT-1: Data Import assignment table with MetadataValidator validation.
3. Fix numeric filter casting bug (in/not_in on Float64/Int columns silently returned empty).
4. Fix filter widget for Int columns (was showing Float values and float step).
5. Fix `between` radio button label doubling ("≤ ≤ inclusive" → "≤ inclusive").
6. Add `breaks_integer: true` support to VizFactory (`scale_x_continuous` / `scale_y_continuous`).
7. Document propagation rules in user guide (PROP-4).
8. Triage and close: STATE-1, STATE-2, BUG-PERF-1, AUDIT-2, AUDIT-3, AUDIT-4.
9. Update and extend UI test checklist.

---

### VizFactory integer axis breaks (VIZ-BREAKS-INT)

`libs/viz_factory/src/viz_factory/scales/core.py`:
- Added `_integer_breaks(lims)` helper: `MaxNLocator(integer=True).tick_values(lims[0], lims[1])`.
- Added `_resolve_continuous_spec(spec)`: pops `breaks_integer` key and substitutes `breaks=_integer_breaks`.
- Both `scale_x_continuous` and `scale_y_continuous` handlers call `_resolve_continuous_spec`.
- Note: `MaxNLocator` cannot be used directly as a plotnine breaks callable (plotnine calls `breaks(limits)` with a tuple). Must be wrapped.

Manifest side: `breaks_integer: true` must be nested under `params:` in the layer entry, not at the layer's top level (VizFactory reads `layer_spec.get('params', {})`).

Applied to: `config/manifests/pipelines/1_test_data_ST22_dummy.yaml` — `year_distribution` plot, `scale_x_continuous` layer.

---

### EXPORT-SGE-2: full_recipe.yaml in single graph export bundle

`app/handlers/single_graph_export_handlers.py`:
- Added section 5: writes `full_recipe.yaml` to ZIP using `cfg.raw_config` (which has `!include` already resolved by the custom SafeLoader in `config_loader.py`).
- Content: plot_id, target_dataset, data_schema, assembly spec, T3 nodes, plot spec.
- README updated to list `full_recipe.yaml` as a bundle artifact (section 6).

---

### EXPORT-SGE-4: multi-file upload hint

`app/handlers/data_import_handlers.py`:
- File upload control includes `"Hold Ctrl/⌘ Cmd to select multiple files"` in helper text.
- Native `multiple=True` already in place — no additional reactive logic needed.

---

### MetadataValidator dtype_map audit (prerequisite for IMPORT-1)

`libs/transformer/src/transformer/metadata_validator.py`:
- Removed `.lower()` from type-string lookup (was masking gaps by normalizing but not helping if key absent).
- Expanded `dtype_map` with all YAML type aliases: `numeric→Float64`, `character→Utf8`, `date→Date`, `integer→Int64`, `bool/boolean→Boolean`, PascalCase aliases (`Int64`, `Float64`, `String`, `Boolean`, `Date`, `Categorical`).
- Added `warnings.warn` for unrecognised type strings (was silently skipping).

Root cause: prior numeric filters cast columns to Utf8 as a workaround (e.g., `Float64` `2022.0` → `"2022.0"` compared to filter value `"2022"` → silent mismatch). Removing the cast exposed the dtype_map gap.

---

### Numeric filter in/not_in casting fix

`app/modules/t3_recipe_engine.py`:
- For `in`/`not_in` on numeric columns: values are now coerced to the column's native dtype using `_coerce_to_dtype()` before calling `is_in()`. No Utf8 cast.
- For string columns: behaviour unchanged (cast column to Utf8, compare string values).

`app/tests/test_filter_operators.py`:
- Synced `_filter_loop` to production fix.
- Added 3 regression tests: `test_in_numeric_int_column`, `test_in_float_column_string_values`, `test_not_in_numeric`. 24/24 passing.

---

### Filter widget fixes

`app/handlers/filter_and_audit_handlers.py`:

1. **Int column widget**: `is_int_col` now computed before `col_min`/`col_max`. Int/UInt columns get `int()` bounds and `step=1` (was always `float()` → showed `2022.0`).
2. **`col_min == col_max` guard**: `col_max = col_min + 1` (was `+ 1.0` → float bleed).
3. **`between` radio labels**: `"≤ inclusive"` / `"< exclusive"` (was doubled symbol `"≤ ≤ inclusive"`).
4. **Reset clears value widget**: `_filter_reset` now calls `ui.update_selectize("fb_value", selected=[])` + `ui.update_text("fb_value", value="")`.
5. **Trash button CSS**: aligned to audit panel trash style — `class_="btn btn-sm btn-link p-0"`, `style="font-size:0.95em; color:#dc3545; line-height:1;"`.

---

### IMPORT-1: Data Import — assignment table + validation

**Design:** Option B — explicit `filename → dataset dropdown` assignment. No filename-convention guessing.

`app/handlers/data_import_handlers.py` (full rewrite):
- `data_import_ui`: embeds `ui.output_ui("data_import_assignment_ui")` inside multi-upload block with Ctrl/⌘ hint.
- `data_import_assignment_ui`: renders one row per uploaded file — filename label + dataset dropdown (`input_select(f"data_import_assign_{i}")`) + Apply button + per-file error display.
- `_handle_multi_upload` effect: captures uploaded files to `_import_pending: reactive.Value[list]`.
- `_handle_apply` effect: for each assignment — runs `MetadataValidator.validate()`, displays inline errors on failure, on success copies file to `source.path`, deletes parquet cache, busts bootloader LF cache, increments `data_refresh_trigger`.
- Parameter: `data_refresh_trigger=None` (injected from server).

`app/src/server.py`:
- Added `data_refresh_trigger = reactive.Value(0)` in shared state block.
- Passed to `_define_home_theater_server`.

`app/handlers/home_theater.py`:
- `define_server` signature: added `data_refresh_trigger=None`.
- `_resolve_t1_lf`: subscribes to trigger — `if data_refresh_trigger is not None: data_refresh_trigger.get()`. All plot renders invalidate after import.
- Passed `data_refresh_trigger` to `define_data_import_server`.

**ADR:** ADR-058 (see architecture_decisions.md).

---

### PROP-4: Propagation documentation

`docs/user_guide/audit_pipeline.qmd`:
- Added "One-at-a-time review workflow" (8-step sequence from staging through per-plot apply).
- Added "Writing good reasons" section with good/weak example pairs.
- Fixed appendix: `"≤ ≤ inclusive"` → `"≤ inclusive"`.

---

### Triage closures

STATE-1, STATE-2, BUG-PERF-1, UX-1, AUDIT-2, AUDIT-3, AUDIT-4, PROP-4, EXPORT-SGE-2, EXPORT-SGE-4, EXPORT-ZIP-STRUCT, VIZ-BREAKS-INT, YEAR-CAST-INT, IMPORT-1 — all moved to completed in `tasks.md`.

---

### Files changed

| File | Change |
|---|---|
| `libs/viz_factory/src/viz_factory/scales/core.py` | `_integer_breaks`, `_resolve_continuous_spec`, both continuous scale handlers updated |
| `libs/transformer/src/transformer/metadata_validator.py` | dtype_map expanded; `.lower()` removed; warn on unknown type |
| `app/modules/t3_recipe_engine.py` | `in`/`not_in` numeric path: coerce values to native dtype |
| `app/tests/test_filter_operators.py` | 3 regression tests; 24/24 passing |
| `app/handlers/filter_and_audit_handlers.py` | Int bounds, `between` labels, reset clears widget, trash CSS |
| `app/handlers/data_import_handlers.py` | Full rewrite — IMPORT-1 assignment table + validation |
| `app/handlers/single_graph_export_handlers.py` | `full_recipe.yaml` added to ZIP; README updated |
| `app/src/server.py` | `data_refresh_trigger = reactive.Value(0)` |
| `app/handlers/home_theater.py` | `_resolve_t1_lf` subscribes to `data_refresh_trigger` |
| `docs/user_guide/audit_pipeline.qmd` | PROP-4: one-at-a-time workflow + good reasons guide |
| `config/manifests/pipelines/1_test_data_ST22_dummy.yaml` | `breaks_integer: true` on year_distribution x-axis |
| `.antigravity/tasks/tasks.md` | Multiple items moved to completed |
| `.antigravity/tasks/tasks_test_ui_current.md` | Sections 19–21 added; SOLVED/CONSIDER markers updated |

---

### Known state after session

- Year distribution x-axis shows integer labels (no `.0` suffix).
- Numeric `in`/`not_in` filters work correctly on Float64 and Int columns.
- Data Import accordion provides assignment table with inline validation errors; imports bust the parquet cache and trigger plot re-render.
- Filter widgets use correct types for Int columns (integer step, no float bleed).
- SGE bundle includes `full_recipe.yaml` with resolved assembly + T3 nodes + plot spec.
- Propagation rules documented in user guide (8-step workflow + reason writing guide).
- Test checklist extended with sections 19 (Data Import), 20 (integer breaks), 21 (filter widget types).

---

### Open after session

- `tasks_test_ui_current.md` sections 9b, 10, 11, 12, 13, 14, 15, 16 — not yet user-tested.
- THEATER-1, UX-NOTIF-1, PROP-2/PROP-3, EXPORT-2/3/4 — deferred.
- Gallery "Clone to Sandbox" re-verify after ADR-057 — deferred.
- ADR-058 and ADR-059 recorded; no further architectural decisions pending.

---

## Session — UX-NOTIF-1: Notification log panel

**Branch:** dev
**Agent:** Claude Sonnet 4.6

---

### Goal

Toast notifications disappear too fast. Add a persistent `🔔 Alerts (N)` accordion in the right sidebar showing the last 20 timestamped user-facing notifications.

---

### Implementation

**New file: `app/handlers/notification_utils.py`**
- `make_notifier(notification_log)` factory — returns `_notify(msg, type="message", duration=5)`.
- `_notify` calls `ui.notification_show` AND appends `{"ts": "HH:MM:SS", "msg": str(msg), "type": type}` to `notification_log`, keeping the last 20 entries.
- Gracefully no-ops when `notification_log=None` (backwards-compatible).

**`app/src/server.py`**
- `notification_log = reactive.Value([])` added in shared state block (alongside `data_refresh_trigger`).
- Passed to `_define_home_theater_server` and `_define_audit_server`.

**`app/handlers/home_theater.py`**
- `notification_log=None` added to `define_server` signature.
- Threaded to all 5 inner define calls: `define_export_server`, `define_session_server`, `define_data_import_server`, `define_single_graph_export_server`, `define_filter_audit_server`.
- `right_sidebar_content_ui`: `ui.output_ui("notification_log_panel_ui")` appended as last element in all 5 branches (Wrangle Studio, Home/Pipeline Audit, Gallery, Test Lab, fallback). The pipeline-persona early return (`if not bootloader.is_enabled("t3_sandbox_enabled"): return ui.div()`) intentionally excluded.
- New `@output @render.ui def notification_log_panel_ui()`: reads log, renders newest-first entries with type-color (`success=#198754`, `warning=#ca6f00`, `error=#dc3545`, `message=#345beb`), 160px max-height scroll, wrapped in `ui.accordion(..., id="notification_log_accordion", open=False)`.

**6 handler files — 24 call sites replaced:**

| File | Calls replaced |
|---|---|
| `app/handlers/filter_and_audit_handlers.py` | 5 |
| `app/handlers/audit_stack.py` | 3 |
| `app/handlers/session_handlers.py` | 9 |
| `app/handlers/export_handlers.py` | 1 |
| `app/handlers/data_import_handlers.py` | 2 |
| `app/handlers/single_graph_export_handlers.py` | 3 |

Each handler adds `from app.handlers.notification_utils import make_notifier; _notify = make_notifier(notification_log)` at the top of its `define_*` function.

**Not replaced** (developer/tool-facing, intentional): `dev_studio.py`, `wrangle_studio.py`, `gallery_handlers.py`, `blueprint_handlers.py`, `ingestion_handlers.py`.

---

### Scope decisions

- **T3 ghost persistence deferred.** The task description mentioned persisting the log to the T3 ghost. This would require `session_manager` access from every handler path; not done in this pass.
- **Plain `ui.notification_show` kept** for developer-internal notifications (Blueprint, Wrangle, Gallery clone) — these are not user-facing and would pollute the log.
- **`notification_log` is purely in-memory** — clears on page refresh. Acceptable for v1; ghost persistence is the v2.

---

### Architectural decision recorded

- **ADR-060** — Notification Log Pattern. See `architecture_decisions.md`.

---

### All imports verified clean (venv Python):
`notification_utils`, `audit_stack`, `session_handlers`, `filter_and_audit_handlers`, `export_handlers`, `data_import_handlers`, `single_graph_export_handlers` — all OK.
