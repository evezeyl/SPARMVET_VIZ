# Audit Log — 2026-04-23

**Agent:** @dasharch (Claude Sonnet 4.6)
**Session Type:** Architecture Design & Documentation
**Status:** Documentation complete. No code changes. Implementation deferred to Phase 21.

---

## Session Summary

Architectural design session to resolve the redundancy between the "Home" and "Analysis Theater (Viz)" dashboard modes, and to define a unified, persona-aware interaction model for the Home Theater. No source code was modified. All decisions were formalized as ADRs and propagated through the full documentation ecosystem.

---

## Decisions Made

### ADR-043: Unified Home Theater

**Trigger:** The "Analysis Theater / Viz" nav item was identified as dead weight — it shared the same `dynamic_tabs()` render function as "Home" and differed only in the header label. The `analysis_groups` manifest tabs were appended identically to both modes.

**Decision:**
- Eliminate "Analysis Theater / Viz" nav item entirely.
- Home becomes the sole results mode. Its tab structure derives **exclusively** from manifest `analysis_groups` (no hardcoded tabs, Inspector tab removed — it has no manifest backing and violates ADR-003/ADR-004).
- Introduce a **Tier Toggle** radio-button strip (T1 Raw / T2 Reference / T3 Wrangling / T3 Plot) replacing `ref_tier_switch` + `view_toggle`. T3 states are persona-gated (≥ `pipeline_exploration_advanced`).
- **Comparison Mode** is a separate, persona-gated toggle (≥ advanced). When ON: 2-column split (T2 reference left, T3 active right). Option A chosen (independent of Tier Toggle).
- Left sidebar filter widgets are **context-reactive to the active plot sub-tab** — regenerated on sub-tab change, scoped to that plot's `plot_spec` aesthetics.
- Plot sub-tabs wrapped in a **collapsible accordion** (default expanded). Data preview in a **separate collapsible accordion below**. Collapse state persists across sub-tab navigation.
- Column picker enforced at `width: 100%`, no multi-row wrapping.
- For lower personas (no T3 access): Tier Toggle shows T1/T2 only; T3 buttons hidden. T3 silently pre-fills from T2 (functionally identical, never displayed).

### ADR-044: Persona-Gated Audit Stack & Right Sidebar Visibility

**Trigger:** For `pipeline_static` and `pipeline_exploration_simple` personas, the right sidebar (Audit Stack) contains no actionable information — T3 ≡ T2 silently, there is no sandbox to inspect. Displaying it wastes screen real estate and misrepresents the interaction model.

**Decision:**
- Right sidebar **hidden entirely** for `pipeline_static` and `pipeline_exploration_simple`. The layout element is excluded (not CSS-hidden) so the theater column expands to fill full width.
- Right sidebar **visible** for ≥ `pipeline_exploration_advanced`: shows T2 Violet blueprint nodes + T3 Yellow sandbox nodes + `btn_apply` + `btn_revert`.
- T3 recipe silently pre-fills from T2 for all personas (protecting plot formatting). Lower personas never see this machinery.
- T2 Violet nodes are visible only when the right sidebar is visible (i.e., only for advanced+ personas who can meaningfully interact with them).

---

## Files Modified

| File | Change |
|---|---|
| `.antigravity/knowledge/architecture_decisions.md` | Added ADR-043 and ADR-044 |
| `.agents/rules/rules_ui_dashboard.md` | Updated §2 (Panel Behaviors), §3 (Persona Reactivity Matrix), §4 (Coding Standards) |
| `.agents/rules/ui_implementation_contract.md` | Updated §2 (Shell Architecture), §5 (Theater Layout Model) |
| `.antigravity/knowledge/project_conventions.md` | Updated §3 (UI Shell Architecture) |
| `.antigravity/plans/implementation_plan_master.md` | Added Phase 21 with sub-phases 21-A through 21-H |
| `.antigravity/tasks/tasks.md` | Added Phase 21 task breakdown as NEXT SESSION |

---

## Implementation Scope (Phase 21 — Not Started)

Primary files to modify during implementation:

- `app/src/server.py` — `dynamic_tabs()`, `sidebar_nav_ui()`, `sidebar_tools_ui()`, `right_sidebar_content_ui()`, filter generation, comparison mode, tier toggle wiring.
- `app/src/ui.py` — CSS: column picker width rules, any layout adjustments.
- No changes to `libs/`, manifests, or data pipeline.

**Gate:** Headless verification via `debug_home_theater.py` (to be created in Phase 21-H) before any live UI testing. Per `ui_manifest_integration_testing.md`.

---

## Open Questions / Deferred

- None outstanding. All design questions from the first session block resolved.

---

## Session Block 2 — Server Decomposition Architecture

### ADR-045: Server Decomposition — Handlers Directory & Manifest Navigator Module

**Trigger:** `app/src/server.py` reached 2,362 lines across five functionally distinct concerns with no structural separation. Phase 21 is about to add more Home Theater logic. Decomposition now prevents further monolith growth.

**Key decision: Option B — `app/modules/manifest_navigator.py`** (not co-located with handlers)

The five pure manifest introspection helpers (`_build_sibling_map`, `_build_schema_registry`, `_build_lineage_chain`, `_load_fields_file`, `_resolve_fields_for_schema`) are extracted to `app/modules/manifest_navigator.py` with public names (drop `_` prefix). Rationale:
- They are pure functions with zero Shiny dependency.
- Needed by the Field Gap Analysis tool (Phase 20), headless debug scripts, and DevStudio.
- Placing them in a handler file would create import hazards (handler files carry `@render.*` side-effects when imported).
- Consistent with the existing `app/modules/` pattern for importable, testable components.

**The Two-Category Law** (architectural lock):
- Pure introspection → `app/modules/` (importable anywhere)
- Shiny wiring → `app/handlers/` (never imported by non-Shiny contexts)

**Five handler modules** created in `app/handlers/`: `home_theater.py`, `audit_stack.py`, `blueprint_handlers.py`, `gallery_handlers.py`, `ingestion_handlers.py`. Each exposes `define_server(input, output, session, *, ...)` with keyword-only dependencies.

**`server.py` target:** ≤ 150 lines — imports, shared state/calcs, shared utils, five delegation calls only.

### Files Created/Modified (Documentation Pass)

| File | Change |
|---|---|
| `.antigravity/knowledge/architecture_decisions.md` | Added ADR-045 |
| `.agents/rules/workspace_standard.md` | Added `rules_app_structure.md` entry to Master Index |
| `.agents/rules/rules_app_structure.md` | **CREATED** — full structural rulebook (ADR-045) |
| `.antigravity/knowledge/project_conventions.md` | Updated §1 File Registry with all new handler/module entries |
| `.antigravity/plans/implementation_plan_master.md` | Added Phase 22 (Server Decomposition, 9 sub-phases) |
| `.antigravity/tasks/tasks.md` | Added Phase 22 task block as priority before Phase 21-B |

### Also in this session: Phase 21-A completed

`server.py` Phase 21-A changes (nav routing simplification) completed before the decomposition design session:
- Removed `"Analysis Theater"` nav item from `sidebar_nav_ui()`.
- Removed `theater_state` reactive and 3 handler functions (`handle_max_plot`, `handle_max_table`, `handle_reset_theater`).
- Rebuilt `dynamic_tabs()` to render exclusively from manifest `analysis_groups` — no hardcoded tabs, no Inspector tab.
- Removed `header_controls` strip (old `btn_max_*`, `view_toggle`, `comparison_mode_toggle_ui`).
- Cleaned `right_sidebar_content_ui()` Home match condition (removed `"Viz"`).

### Implementation Order Confirmed

Phase 22 (decomposition) → Phase 21-B onward (new Home Theater features into correct files).

---

## Session Block 3 — Phase 22 Implementation (Server Decomposition)

**Status:** COMPLETE. All code changes implemented and verified. UI smoke test passed (no major loss detected by user).

### What was done

Phase 22 extracted 2,362-line `server.py` into 6 new files following the Two-Category Law (ADR-045):

| New file | Lines | Concern |
|---|---|---|
| `app/modules/manifest_navigator.py` | ~280 | 5 pure public functions (zero Shiny dependency) |
| `app/handlers/__init__.py` | ~20 | Package docstring + Two-Category Law reminder |
| `app/handlers/gallery_handlers.py` | ~200 | Gallery filtering, preview, clone, submission |
| `app/handlers/ingestion_handlers.py` | ~55 | `handle_ingest`, `update_persona_context` |
| `app/handlers/audit_stack.py` | ~100 | T2/T3 audit nodes, `handle_apply`, `recipe_pending_badge_ui` |
| `app/handlers/blueprint_handlers.py` | ~450 | Manifest import, TubeMap, Lineage Rail, upload/save/download |
| `app/handlers/home_theater.py` | ~290 | `dynamic_tabs`, sidebars, filters, plots, tables, `right_sidebar_content_ui` |

`app/src/server.py` slimmed: **2,362 → 228 lines** (thin orchestrator only).

### Verification

- `.venv/bin/python -c "from app.modules.manifest_navigator import build_sibling_map; print('OK')"` → OK
- All 6 new modules imported cleanly in one shot.
- User performed live UI smoke test — no major loss detected.

### Key implementation decisions

- `_includes_map`, `_component_ctx_map`, `_schema_registry` reactive values declared early in `server()` (before `wrangle_studio.define_server()`) so lambdas can capture them by name before the blueprint handler registers them.
- `show_sparmvet_error` and `primary_keys()` removed from server.py (no remaining callers after extraction).
- Five private function bodies (`_build_sibling_map` etc.) deleted from server.py; 14 call sites in the blueprint handler use public names directly.
- `comparison_mode_toggle_ui` moved to `home_theater.py` (deferred full implementation to Phase 21-E).

### Files modified

| File | Change |
|---|---|
| `app/src/server.py` | Slimmed to 228 lines — shared state, calcs, utils, 5 delegation calls |
| `app/modules/manifest_navigator.py` | **CREATED** — 5 pure public functions |
| `app/handlers/__init__.py` | **CREATED** — package init |
| `app/handlers/gallery_handlers.py` | **CREATED** |
| `app/handlers/ingestion_handlers.py` | **CREATED** |
| `app/handlers/audit_stack.py` | **CREATED** |
| `app/handlers/blueprint_handlers.py` | **CREATED** |
| `app/handlers/home_theater.py` | **CREATED** |


---

## Session Block 4 — Sequential Lineage Build (Phase 1)
**Agent:** Antigravity (Bio-scientist persona)
**Status:** Lineage 1 [COMPLETE]

### Decisions & Implementation
- **Persona Context**: Adopted Bio-scientist persona to guide manifestation development with biological focus.
- **Workflow Strategy**: Sequential lineage building (A to Z) with materialization to timestamped tmp directories for user review.
- **Lineage 1 (AMR Profiling)**:
  - Joined ResFinder with Metadata.
  - Implemented multi-resistance annotation: isolates with ≥2 distinct phenotypes are flagged as .
  - Defined detection thresholds: 90% identity, 60% overlap.
- **Manifest Standards**: Created `2_test_data_ST22_dummy.yaml` following ADR-041 (Mirrored Directory Deconstruction).

### Files Created/Modified
| File | Change |
|---|---|
| `config/manifests/pipelines/2_test_data_ST22_dummy.yaml` | Created new official manifest |
| `config/manifests/pipelines/2_test_data_ST22_dummy/` | Populated with decomposed fragments |
| `tmp/2026-04-23/AMR_Profile/step1_wrangling/AMR_Profile_Joint.tsv` | Materialized assembly |
| `tmp/2026-04-23/AMR_Profile/step2_plots/.../multi_resistance_distribution.png` | Materialized visualization |

### Next Steps
- Start **Lineage 2: Plasmid Dynamics** (PlasmidFinder + Metadata).
- Verify incompatibility group distribution.

---

## Technical Meta-Analysis: Session Friction Points

### 1. Inexplicit Manifest Rules
- **"Where was the gene column filtered?"**: Highlighted that the `final_contract` as a **Strict Projection Guard** was not explicitly documented.
- **"Why is Country not materializing?"**: Showed a conflict between the engine's implicit dropping of columns and the user's expectation of a "pass-through" behavior.
- **"How is sequence_type included while not in raw data?"**: Demonstrated that **Resilient/Silent Ingestion** (ADR-013) can mask intent, leading to confusion.

### 2. Implicit Knowledge Applied (Not in Protocol)
- **ADR-041 Compliance**: Executed the "Mirrored Directory" standard (nested fragments in `basename/` folders) which is currently only in structural rules, not testing guides.
- **YAML Quoting Nuances**: Applied manual quoting to the `on` keyword in joins (reserved boolean in YAML).
- **Library Script Discovery**: Manually mapped the `@verify` protocol to `debug_wrangler.py` and `debug_assembler.py` in the `libs/transformer/tests` directory.

### 3. Missing Structural Instructions
- **Scientific Audit Mandate**: Lack of an explicit instruction to automatically materialize Tier 1 (Atomic Wrangling) for download/audit of raw cleaning.
- **Biological Typing Standard**: Need for a rule that discrete variables (Year, ST) must be cast to `Int` or `String` in Tier 2 to satisfy Plotnine aesthetic requirements.
- **Whitelisting Policy**: Clarification needed that manifests are *exclusive* (only listed columns survive).

---

## Session Block 5 — @deps Full Project Scan & Annotation

**Agent:** @dasharch (Claude Sonnet 4.6)
**Status:** COMPLETE. All qualifying files annotated. Graph regenerated.

### What was done

Continued from Session Block 4 (previous context). Executed the mandatory full-project scan mandated by `workspace_standard.md §5-E (SESSION LAW)`.

**Total: 50 files annotated, 98 edges mapped.**

### Files annotated in this block

| File | @deps type | Key provides |
|---|---|---|
| `libs/transformer/src/transformer/actions/cleaning/core.py` | Python block | 17 cleaning actions |
| `libs/transformer/src/transformer/actions/cleaning/analytical.py` | Python block | 21 analytical actions |
| `libs/transformer/src/transformer/actions/cleaning/advanced.py` | Python block | 4 advanced actions |
| `libs/transformer/src/transformer/actions/relational/joins.py` | Python block | action:join, action:join_filter |
| `libs/transformer/src/transformer/actions/reshaping/core.py` | Python block | 7 reshaping actions |
| `libs/transformer/src/transformer/actions/performance/aggregation.py` | Python block | action:summarize, action:sort, action:count_by_group |
| `libs/transformer/src/transformer/actions/persistence/anchor.py` | Python block | action:sink_parquet, action:scan_parquet |
| `libs/transformer/src/transformer/actions/base.py` | Python block | decorator:register_action, registry:AVAILABLE_WRANGLING_ACTIONS |
| `libs/transformer/src/transformer/metadata_validator.py` | Python block | class:MetadataValidator |
| `app/modules/manifest_navigator.py` | Python block | 5 pure introspection functions |
| `app/modules/wrangle_studio.py` | Python block | class:WrangleStudio |
| `app/modules/dev_studio.py` | Python block | class:DevStudio |
| `app/handlers/home_theater.py` | Python block | function:define_server (home_theater) |
| `app/handlers/blueprint_handlers.py` | Python block | function:define_server (blueprint) |
| `app/handlers/audit_stack.py` | Python block | function:define_server (audit) |
| `app/handlers/gallery_handlers.py` | Python block | function:define_server (gallery) |
| `app/handlers/ingestion_handlers.py` | Python block | function:define_server (ingestion) |
| `libs/utils/src/utils/blueprint_mapper.py` | Python block | class:BlueprintMapper, constant:_CY_COLOURS |
| `libs/utils/src/utils/config_loader.py` | Python block | class:ConfigManager |
| `libs/utils/src/utils/hashing.py` | Python block | function:generate_config_hash |
| `libs/viz_factory/src/viz_factory/registry.py` | Python block | decorator:register_plot_component, registry:PLOT_COMPONENTS |
| `libs/viz_factory/src/viz_factory/viz_factory.py` | Python block | class:VizFactory, method:render |
| `libs/viz_factory/src/viz_factory/geoms/core.py` | Python block | 66 geom/stat components |
| `libs/viz_factory/src/viz_factory/themes/core.py` | Python block | 23 theme/element/labs components |
| `libs/viz_factory/src/viz_factory/scales/core.py` | Python block | ~30 scale components |
| `libs/viz_factory/src/viz_factory/positions/core.py` | Python block | 8 position components |
| `libs/viz_factory/src/viz_factory/guides/core.py` | Python block | 11 guide components |
| `libs/viz_factory/src/viz_factory/facets/core.py` | Python block | 9 facet components |
| `libs/viz_factory/src/viz_factory/coords/core.py` | Python block | 6 coord components |
| `.agents/rules/rules_app_structure.md` | YAML frontmatter deps | rule:app_structure_law, rule:two_category_law |
| `.agents/rules/rules_verification_testing.md` | YAML frontmatter deps | rule:verify_protocol, rule:tmpAI_tmp_segregation |
| `.agents/rules/rules_runtime_environment.md` | YAML frontmatter deps | rule:venv_path_mandate |
| `.agents/rules/rules_ui_dashboard.md` | YAML frontmatter deps | rule:ui_orchestration, rule:theatre_layout |
| `.agents/rules/rules_documentation_aesthetics.md` | YAML frontmatter deps | rule:violet_law, rule:quarto_mermaid_standards |
| `.agents/rules/rules_data_engine.md` | YAML frontmatter deps (added) | rule:3tier_lifecycle, rule:wrangling_block_structure |
| `.agents/rules/rules_viz_factory.md` | YAML frontmatter deps (added) | rule:artist_pillar, rule:plotnine_parity |
| `config/manifests/pipelines/2_test_data_ST22_dummy.yaml` | YAML @deps comment | manifest:2_test_data_ST22_dummy |
| `config/manifests/pipelines/1_test_data_ST22_dummy.yaml` | YAML @deps comment | manifest:ST22_dummy_manifest |

### @deps verification: already annotated files (unchanged)

All files annotated in the previous session block remain correct:
- `app/modules/orchestrator.py`, `libs/transformer/tests/debug_assembler.py`
- `libs/transformer/src/transformer/data_assembler.py`, `data_wrangler.py`
- `libs/transformer/src/transformer/actions/cleaning/expressions.py`
- `libs/ingestion/src/ingestion/ingestor.py`
- `libs/viz_factory/tests/debug_gallery.py`
- `config/manifests/pipelines/2_test_data_ST22_dummy/assembly/AMR_Profile_Joint.yaml`
- `.agents/rules/rules_manifest_structure.md`, `.agents/rules/rules_persona_bioscientist.md`
- `.agents/rules/workspace_standard.md`

### Graph stats after regeneration

```
.venv/bin/python assets/scripts/build_dep_graph.py
→ 50 nodes · 98 edges · clean run
```

---

## Session Block 6 — @deps Continuation: Remaining App Modules & Libs

**Agent:** @dasharch (Claude Sonnet 4.6)
**Trigger:** Session resumed after context compaction — completing full project scan started in Block 5.

### Files annotated (9 files)

| File | @deps type | Key provided contracts |
|------|-----------|------------------------|
| `app/modules/exporter.py` | Python block | class:SubmissionExporter |
| `app/modules/gallery_viewer.py` | Python block | class:GalleryViewer |
| `app/modules/help_registry.py` | Python block | module:help_registry_ui, module:help_registry_server |
| `app/modules/persona_manager.py` | Python block | class:PersonaManager |
| `libs/transformer/src/transformer/lookup.py` | Python block | function:lookup_anchor_rows |
| `libs/transformer/src/transformer/pipeline.py` | Python block | class:PipelineExecutor |
| `libs/transformer/src/transformer/registry.py` | Python block | function:get_action_function |
| `libs/utils/src/utils/errors.py` | Python block | class:SPARMVET_Error, class:IngestionError, class:TransformationError, class:VisualizationError |
| `libs/utils/src/utils/gallery_manager.py` | Python block | class:GalleryManager |

### Graph stats after regeneration

```
python assets/scripts/build_dep_graph.py
→ 59 nodes · 112 edges · clean run
```

### Status

Full project scan complete. All qualifying Python and YAML files across `app/`, `libs/`, `config/`, and `.agents/rules/` now carry @deps annotations. `__init__.py` files intentionally skipped (no contracts to declare).

---

## Session Block 7 — Bioscientist Persona Hardening

**Agent:** @dasharch (Claude Sonnet 4.6)
**Trigger:** User request to harden `rules_persona_bioscientist.md` based on observed failures during manifest development session with another agent.

### Problems identified (from Manifest_dev_what_have_we_learned.md)

1. **Column Projection Law**: `final_contract` role as strict whitelist was not explicit enough — agents assumed pass-through for unlisted columns.
2. **Silent Skip Confusion**: Resilient ingestion masks missing fields. No explicit rule to check TSV headers against `input_fields`.
3. **Type Casting Ambiguity**: Float64→String producing "2022.0" — reasoning was present but cause (TSV infers Float64 for all numerics) was not stated.
4. **Missing pointer to `project_conventions.md`**: Agents had to discover debug scripts by scanning the filesystem.
5. **No handoff protocol**: No structured mechanism to request Python changes from `@dasharch`. Agents either tried to modify Python or got stuck.
6. **No sequential build law**: Agents generated complete manifests in one pass, creating cascading failures hard to diagnose.
7. **Python boundary too vague**: "NEVER modify .py files" was not enforced with examples; ambiguous edge cases (typos, comments) were not covered.

### Changes made to `rules_persona_bioscientist.md`

| Section | Change |
|---|---|
| Prohibition §6 | Hardened: "not even to fix a typo, add a comment, or change an import" |
| §0-B | New: Python Code Boundary — decision table for every Python-adjacent situation |
| §1 | Added `project_conventions.md` as mandatory read with description of what it governs |
| §2 Interview | Added "column carry-over policy" as mandatory interview question |
| §3-A | Added Silent Skip Warning — check TSV headers before writing wrangling steps |
| §3-B | Added join key dtype mismatch note and Float64 cause explanation |
| §3-C | Added `target_dataset` case-sensitivity note |
| §3-E | Added identity column retention guidance |
| §3-F | **New section**: Sequential Build Law — one step per response, gate structure, truth checks per step, consequence-stating requirement |
| §4-A | Enhanced Enhancement Request with "Requested by" field |
| §4-B | **New section**: @dasharch Handoff Protocol — structured handoff format, task.md entry format, what @dasharch does with it |
| §5 | Added `final_contract` identity column justification requirement |
| §6 checklist | Added TSV header inspection check |
| §7 | Added `--tmp` and `--output_root` flags with dated routing (`tmp/YYYY-MM-DD/<lineage>/`) |
| §7 | Added error diagnosis table (ColumnNotFoundError, SchemaError, row explosion, etc.) |
| §7 | Added "if debug script itself has a bug → handoff" rule |

---

## Session Block 8 — Task Hygiene, Infrastructure Upgrades & Phase 21-C

**Agent:** @dasharch (Claude Sonnet 4.6)
**Trigger:** Session resumed after compaction. Task hygiene requested followed by next task execution.

### Task Hygiene

- Consolidated `tasks.md`: archived completed phases 16–22 to single header block.
- Marked as completed: IU-4 (Ingestor diagnostics), IU-5 (@deps annotation), IU-6 (Bioscientist hardening), Phase 21-A, Phase 21-B.
- Structured pending work into clear sections: Phase 21-C through 21-H, IU-1 through IU-3, Active Lineage, Deferred Backlog.

### IU-1: VizFactory — position, labels, guides

**File changed:** `libs/viz_factory/src/viz_factory/viz_factory.py`

- `_standardize_config`: `factory_id` now injects base geom even when `layers` is present (prepended at index 0, only if geom name not already in layers). Fixes `factory_id: bar_logic` + `layers: [position_dodge, labs]` pattern used in `multi_resistance_*.yaml`.
- `render`: replaced flat `title` handling with structured `labels` block → `labs(**labels_block)`.
- `render`: added `guides` block → `guides(fill=guide_legend(...))` with `guide_colorbar` fallback. Supports `false`/"none" to suppress a guide.
- Verified: `_standardize_config` produces `[geom_bar, position_dodge, labs]` for dodge bar charts.

### IU-2: DataAssembler — shorthand normalization

**File changed:** `libs/transformer/src/transformer/data_assembler.py`

- Added `DataAssembler._normalize_recipe(recipe)` static method.
- Recognizes shorthand steps (`{join: {...}}`, `{select: [...]}`, `{mutate: {...}}`) and expands to `{action: ..., ...}` canonical form.
- Called at top of `assemble()` before ADR-024 hash computation.
- Canonical steps (with `action` key) pass through unchanged — no regression.
- Verified with unit test: join + select shorthand both expand correctly.

### IU-3: Contract-Aware Materialization

- Confirmed: `debug_gallery.py` reads contracted output as Parquet (types preserved).
- `debug_assembler.py` writes contracted Parquet; no code path re-reads contracted TSV through ingestor.
- Risk is theoretical; no code change needed. Marked complete.

### Phase 21-C: Tier Toggle

**Files changed:** `app/src/server.py`, `app/handlers/home_theater.py`

| Change | Detail |
|---|---|
| `server.py` | Added `tier_toggle = reactive.Value("T1")` |
| `server.py` | `tier_reference`: checks `tier_toggle.get() in ("T2", "T3")` instead of dead `ref_tier_switch` |
| `server.py` | `tier3_leaf`: checks `tier_toggle.get() == "T3"` instead of dead `view_toggle` |
| `server.py` | `tier_toggle` passed to `_define_home_theater_server()` |
| `home_theater.py` | `define_server` signature updated to accept `tier_toggle` |
| `home_theater.py` | `_track_tier_toggle` reactive Effect syncs `input.tier_toggle` → `tier_toggle.set(val)` |
| `home_theater.py` | Theater header: `ui.input_radio_buttons("tier_toggle", ...)` with T1/T2 always; T3 persona-gated (advanced+/independent/developer) |
| `home_theater.py` | Header layout: branding left, tier strip right, flex justify-content-between |

**Verified:** `server.py` and `home_theater.py` import cleanly. No regressions.

### @deps verification

All changed files carry existing @deps annotations — no new annotations needed for this session (no new contracts added, only internal logic changes).

### Graph stats

Not regenerated (no new @deps nodes added). Current state: 59 nodes · 112 edges (unchanged).

---

## Session 3 — Phase 21-F: Context-Reactive Filters & Column Selector

**Agent:** @dasharch (Claude Sonnet 4.6)
**Trigger:** Continuation of Phase 21 implementation. User requested filter recipe builder and column selector for the Home Theater data preview.

### Design Decisions (2026-04-23)

**Filter recipe builder pattern** (Phase 21-F):
- Two surfaces: Surface A = left sidebar row filters (affect plots + preview); Surface B = data preview column selector (preview-only).
- Row filters: recipe builder — add N rows of `{column, op, value, dtype}`. Apply commits `_pending_filters` → `applied_filters`. Multiple rows same column = AND logic.
- Type handling: Year/ST stay `Int64`. Plot manifests use `scale_x_discrete` to declare categorical intent. Filter builder reads active plot spec — if `scale_x_discrete` present for that column → multi-select; else → range/text. No data mutation. Coercion at filter time (column cast to Utf8 for string set ops).
- T3 integration deferred (21-F-5): UI stub present; recipe step serialization deferred.
- `not_in` op added to VizFactory predicate pushdown (IU-6, viz_factory.py).

**Shiny reactive stability law** (discovered during implementation):
- A `@render.ui` that reads a `reactive.Value` re-renders its entire DOM subtree when the value changes. Any child `output_ui` DOM nodes are destroyed and cannot re-bind.
- **Solution**: Shell renders read only slowly-changing reactives (persona only). Sub-outputs (`filter_rows_ui`, `filter_form_ui`, `filter_controls_ui`, `filter_t3_btn_ui`) are independent `@output @render.ui` functions, each reading only the reactive values they actually need.
- `selected=` must be passed explicitly to `ui.input_select()` inside a `@render.ui` to preserve user choice across re-renders.

**Column selector `w-100` + CSS-only width fix:**
- `class_="mb-2 w-100 column-picker-container"` on wrapper div.
- `.column-picker-container .selectize-control { width: 100% }` in CSS.
- `overflow: visible` on `#acc_home_data .accordion-body` to prevent dropdown clip.
- JS `render` callbacks removed (caused `l.apply is not a function` error in selectize.js — render option requires actual JS function objects, not Python strings).

### Bugs Fixed

| Bug | Root Cause | Fix |
|-----|-----------|-----|
| Column selector resets to first column on Add | `sidebar_filters` shell read `_pending_filters` → full re-render destroyed `fb_col` input | Split into shell (persona-only) + independent sub-outputs |
| `l.apply is not a function` JS error | selectize `render` option passed Python string, not JS function | Removed JS render callbacks; CSS handles font size |
| `cannot cast List type (inner: 'String', to: 'String')` | Polars: `col == ["a","b"]` invalid; `is_in(["str"])` on Int64 column tried List[String]→String cast | Auto-promote `eq`→`in` when value is list; cast column to Utf8 before `is_in()` |
| Empty table when no values selected in selectize | Empty tuple `()` → `is_in([])` matches nothing | `if not value: return` guard in `_filter_add_row` before appending |
| Filter selector menu disappeared | Shell read `_pending_filters`/`applied_filters` → re-rendered on every row add → destroyed child `output_ui` DOM | Shell reads only `current_persona.get()` |
| System Tools disappeared | `system_tools_ui` missing `@output` decorator | Added `@output` above `@render.ui` |
| Tier toggle resets active plot tab | `selected=tier_toggle.get()` inside `dynamic_tabs` read reactive → tier change triggered full DOM re-render | Changed to `selected="T1"` (static) — `_track_tier_toggle` effect syncs separately |
| Empty table / no plots when no `analysis_groups` | Early return path didn't mount `home_data_preview` slot; no fallback to top-level `plots` | `data_preview_section` built before `if not groups:` check; fallback wraps top-level plots in synthetic navset |

### Files Modified

| File | Change |
|------|--------|
| `app/handlers/home_theater.py` | Phase 21-F: filter recipe builder (sidebar_filters shell + 4 sub-outputs + 5 effects), home_data_preview with filter+col-selector, home_col_selector_ui, _apply_filter_rows helper, _spec_discrete_axes helper, _resolve_active_spec/lf helpers. @output fix for system_tools_ui. tier_toggle selected="T1" static. no-groups fallback + data_preview_section always mounted. |
| `libs/viz_factory/src/viz_factory/viz_factory.py` | Added `not_in` op to predicate pushdown (IU-6) |
| `app/src/ui.py` | CSS: `.column-picker-container` width rules, `#acc_home_data .accordion-body { overflow: visible }`, `#acc_home_data .shiny-html-output { width: 100% }` |
| `.antigravity/tasks/tasks.md` | Phase 21-F complete (21-F-1 through 21-F-4, 21-F-6); 21-F-5 and 21-F-7 deferred |

---

## Session 3 — Phase 21-I: Export Results Bundle

**Agent:** @dasharch (Claude Sonnet 4.6)
**Trigger:** User confirmed export feature design: System Tools location, Python Quarto, all plots, ghost save deferred.

### Design Decisions (2026-04-23)

**Export bundle spec** (ADR-021 extension, cf. UI_Definition.md + UI_AI_Discussion.md):
- Location: System Tools accordion in left sidebar.
- User provides: `export_user_name` text field (sanitized), `export_preset` radio (web/publication).
- Output: `YYYYMMDD_HHMMSS_<name>_results.zip` downloaded via browser.
- "No Trace No Export" rule: if `applied_filters.get()` non-empty → `FILTERS.txt` embedded in bundle with full filter trace.
- Quarto: `.qmd` source always generated (Python Quarto can be bundled in Docker environment).
- Plot selection deferred — bundle always includes all plots.
- Ghost save deferred — save location = `user_sessions` in `config/connectors/local/local_connector.yaml`.

**Tier-aware data export policy:**
- T1: always exported (assembled anchor).
- T2: always exported (analysis-ready reference via `tier_reference()`).
- T3: only for advanced+ persona (`pipeline_exploration_advanced`, `project_independent`, `developer`) AND when `tier_toggle == "T3"` is active. Deferred if no active T3.
- Files named `<dataset>_T1.tsv`, `<dataset>_T2.tsv`, `<dataset>_T3.tsv`.

**Recipe export:**
- All YAML files from active project manifest directory (rglob `*.yaml`).
- T3 updated recipe serialization deferred (21-F-5); when implemented, updated recipe steps will be written alongside static YAMLs.

### Bundle contents

| Path | Contents |
|------|---------|
| `plots/` | SVG (web preset) or PNG ≥600 DPI (publication) per plot; error stub on render failure |
| `data/` | `<dataset>_T1.tsv`, `<dataset>_T2.tsv` always; `<dataset>_T3.tsv` for advanced+T3 |
| `recipes/<proj>/` | All YAML wrangling/assembly/plot files from project manifest dir |
| `FILTERS.txt` | Filter trace when `applied_filters` non-empty (No Trace No Export compliance) |
| `report.qmd` | Quarto source — YAML front-matter, filter table (if filters), figure includes, data section with tier listing |
| `README.txt` | Bundle manifest: timestamp, project, persona, preset, tiers exported, counts |

### Files Modified

| File | Change |
|------|--------|
| `app/handlers/home_theater.py` | `system_tools_ui` rebuilt with export controls (name input, preset radio, warning, download button). `@render.download export_bundle_download` handler (async generator, yields zip bytes). `_export_bundle_filename()` helper for reactive-safe filename. |
| `.antigravity/tasks/tasks.md` | Added Phase 21-I as COMPLETED |

### Deferred / Not implemented

- Ghost save / session restore to `user_sessions`.
- Per-plot checkbox selection (all plots always exported for now).
- T3 recipe serialization (21-F-5 — serialize filter steps to YAML recipe).
