## Session Log — 2026-04-30 — @dasharch

---

## Session Block 1 — 2026-04-30 — @dasharch — Project Audit Response (Documentation & Technical Debt)

### Context
A separate agent with a large context window performed a full project state audit (`EVE_WORK/daily/2026-04-30/Project_full_audit.md`). This session verified each finding and resolved all confirmed issues.

### Audit Findings & Verdicts

| ID | Finding | Verdict | Resolution |
|---|---|---|---|
| DOC-1 | `implementation_plan_master.md` out of sync (Phase 21-E/G/H stale, Phase 22-J absent) | Funded | DONE |
| DOC-2 | ADR-040 header lists 18-D and 18-F as pending, but body marks them ✅ COMPLETED | Funded (trivial) | DONE |
| DOC-3 | ADR-029a still describes deprecated state vars as live design | Funded | DONE |
| DOC-4 | ADR-006 deprecation note missing ADR-032 `assets/scripts/` nuance | Partially funded — H-2 already covered it | No new action |
| BUG-1 | `build_dep_graph.py` parses `@deps` inside fenced code blocks as real declarations | Funded | DONE |
| ARCH-1 | `home_theater.py` 2,547 lines — past ADR-045 threshold; needs Phase 24 design | Funded | DONE (design only) |

### Changes Made

**Commit `366390c` — docs(audit): fix documentation drift**
| File | Change |
|---|---|
| `.antigravity/knowledge/architecture_decisions.md` | ADR-040 header corrected (18-D/F removed from pending). ADR-029a marked SUPERSEDED by ADR-043; removed vars listed. |
| `.antigravity/plans/implementation_plan_master.md` | Phase 21-E/G/H marked COMPLETED. Phase 22-J added as completed sub-phase. Phase 11 numbering gap explained. Phase 21 header updated to COMPLETED. |
| `.antigravity/tasks/tasks.md` | H-4 escalated to 2,547-line count. DOC-1/2/3 added and marked done. ARCH-1 and BUG-1 added. |

**Commit `83e227f` — design(24) + fix(dep-graph)**
| File | Change |
|---|---|
| `.antigravity/plans/implementation_plan_master.md` | Phase 24 design entry written (sub-phases 24-A through 24-E). |
| `.antigravity/knowledge/architecture_decisions.md` | ADR-051 written: decomposition boundary rule, 4-file map, shared state protocol, pre-condition gates. |
| `assets/scripts/build_dep_graph.py` | Strip fenced code blocks before `@deps` regex. `workspace_standard.md` false positive eliminated. |
| `.antigravity/knowledge/dependency_index.md` | Regenerated — 89 nodes, 151 edges, clean. |
| `.antigravity/tasks/tasks.md` | ARCH-1 and BUG-1 marked done. |

### Phase 24 Design Summary (ADR-051)

`home_theater.py` (2,547 lines) will be split into:
- `home_theater.py` → ~900 lines (layout, routing, plot renders)
- `t3_audit_handlers.py` → ~450 lines (filter effects, propagation modal)
- `session_handlers.py` → ~400 lines (session list, import, restore/delete)
- `export_handlers.py` → ~510 lines (export bundle, audit reports)
- `t3_recipe_engine.py` (module) → ~120 lines (pure filter/drop helpers)

**Gate:** Implementation blocked until Phase 22-J live-UI sign-off and ST22 Lineage 2 verified.

---

## Session Block 2 — 2026-04-30 — @dasharch — Phase 23-A: Directory Rename & Bootloader Extension

### Context
Phase 23-A: rename `config/connectors/` → `config/deployment/` and extend `bootloader.py` with the 4-level `SPARMVET_PROFILE` resolution chain (ADR-048 §4). Full impact analysis performed before any file edits.

### Impact Analysis (pre-implementation)
Code files referencing `config/connectors/` (only 4):
- `app/src/bootloader.py:43` — path construction
- `config/connectors/templates/connector_template.yaml:13,16` — comments
- `assets/scripts/create_test_deployment.py:4,13,45,57` — output path
- No external callers reference `bootloader.connector` or `bootloader.connector_config` — all use public methods (`get_location`, `get_script_path`, `get_python_path`, `get_default_project`).

### Changes Made

**`app/src/bootloader.py`** — full rewrite (same public API):
- `_resolve_profile_path()` — 4-level chain with inline resolution order docs
- `_validate_profile()` — validates all 5 required location keys
- `__init__` — `SPARMVET_PROFILE` resolution, startup log, `default_persona` applied
- `get_location()` — `project_root`-aware path resolution
- Cache key changed from connector name to resolved absolute path string
- New attributes: `deployment_level`, `deployment_type`, `deployment_name`, `default_manifest`, `default_persona`, `project_root`

**`config/connectors/` → `config/deployment/`** — git mv
**`local_connector.yaml` → `local_profile.yaml`** — git mv; added `deployment_type: filesystem`, `deployment_name`

**`config/deployment/templates/connector_template.yaml`** — removed stale "Phase 23 rename pending" note; updated level-4 path

**`assets/scripts/create_test_deployment.py`** — updated all 4 path references

**`.antigravity/knowledge/project_conventions.md`** — §4 Path Authority rewritten with 4-level resolution table (user request: document the chain so future agents find it)

### Verification
- Import check: `Bootloader` resolves at level 4, all attributes correct.
- Full app import (`from app.src.main import app`): clean (same pre-existing warnings only).

### Key Decisions / Notes
- `project_root` is optional — local dev profile omits it; paths resolve relative to CWD as before.
- `SPARMVET_PROFILE` pointing to a non-existent file is a **hard error** (misconfiguration), not a fall-through.
- `default_persona` in profile overrides `SPARMVET_PERSONA` env var, but explicit `persona=` kwarg to `Bootloader()` takes highest priority.
- Phase 23-B (connector library) remains pending.


---

## Session Block 3: Phase 23-B — Connector Library (2026-04-30)

**Objective**: Implement ADR-048 §5 adapter pattern — four connector classes for filesystem, Galaxy, and IRIDA deployments.

**Changes Made**

`libs/connector/src/connector/base.py` — NEW: `BaseConnector` ABC with `resolve_paths()`, `fetch_data()` abstract methods; `get_manifest_path()`, `get_default_persona()`, `get_deployment_name()`, `get_deployment_type()` concrete methods.

`libs/connector/src/connector/filesystem.py` — NEW: `FilesystemConnector` — `project_root`-aware path resolution; `fetch_data()` no-op.

`libs/connector/src/connector/galaxy.py` — NEW: `GalaxyConnector(FilesystemConnector)` — falls back to `_GALAXY_JOB_HOME_DIR` / `GALAXY_SLOTS_DIR` env vars when profile `project_root` absent. `fetch_data()` no-op (Galaxy mounts data before app starts).

`libs/connector/src/connector/irida.py` — NEW: `IridaConnector(FilesystemConnector)` — resolves paths via `irida.local_cache` as effective `project_root`; `fetch_data()` validates `SPARMVET_IRIDA_TOKEN` env var + `irida` block, then raises `NotImplementedError` (Phase 23-D stub).

`libs/connector/src/connector/__init__.py` — rewritten: public API + `get_connector(profile)` factory routing `deployment_type` → class.

`libs/connector/tests/test_connectors.py` — NEW: 31 unit tests covering all connectors, factory routing, error branches (env var injection via `monkeypatch`). No filesystem access required.

### Verification
- Each connector verified individually via in-line Python scenarios before test suite.
- Full pytest: **31/31 passed in 0.08s**.
- Full app import (`from app.src.main import app`): clean (same pre-existing warnings only).

### Key Decisions / Notes
- Implementation placed inside existing `libs/connector/` (singular) — already editable-installed in venv. ADR-048 says `libs/connectors/` (plural) but creating a duplicate package was rejected; ADR-048 updated to match reality.
- Legacy `galaxy_connector.py` (BioBlend Galaxy API client) coexists alongside new `galaxy.py` (deployment adapter) — different concerns, no conflict.
- Phase 23-D (`IridaConnector.fetch_data()` full implementation) gated on IRIDA plugin confirmation from ops team.

---

## Session Block 4: Demo prep — operator contract, propagation, persona refactor (2026-04-30)

**Objective**: Make the build demo-ready. Fix surfaced bugs, lock down the filter operator contract with regression tests, document export/persona behaviour clearly, and start the persona-flag refactor without changing user-visible behaviour.

### Demo blockers — all fixed and committed individually for safe rollback

| ID | Commit | Resolved |
|---|---|---|
| DEMO-1 | `55ab1c5` | viz_factory rotation KeyError on Virulence Variants |
| DEMO-2 | `b91dfc9` + `33afa1b` | Quast unpivot to tier1 + register `boxplot_logic` |
| DEMO-3 | `4c962e6` | `_apply_filter_rows` reads actual LF dtype, coerces string operands |
| DEMO-4 | `8b4f3a4` | viz_factory plot filter — same dtype-aware coercion |
| BUG-CACHE-1 | `09366fc` | Upstream provenance hash invalidates cached parquet on manifest/source-file changes |
| DATA-1 | `4133159` | Aligned Quast/FastP/Bracken/Quality_metrics sample_ids to metadata canonical set |

### Filter pipeline overhaul

- **UX-FILTER-1** (`f08b88f` `61e86a8` `f4b1a91` `5c5083f`): dtype-aware filter widgets. Numeric scalar ops → `ui.input_numeric`. New `between` op → two numeric inputs (replaced the slider after render-glitch + locale-separator + imprecise-input issues). Inclusivity toggle (`≤ ≤` / `< <`). fb_op dropdown preserves selection across re-renders.
- **AUDIT-1** (`3c6195f`) — ADR-049 amendment: PK-column filtering ALLOWED (no more silent conversion to exclusion_row). Operator is the truth; PK warning is the safety rail. ADR-049a recorded.
- **PROP-1** (`b4dcd10`): propagation modal shows per-plot column-presence preview before confirm. Three states ✅/⚠️/❓.
- **283207c**: 21-case regression test for filter operators (`app/tests/test_filter_operators.py`) + user docs (`docs/user_guide/audit_pipeline.qmd` propagation section + filter-operators reference appendix) + ADR-049a amendment.

### Architectural & cosmetic

- **PERSONA-1a** (`870f0e6`): refactored sidebar_nav_ui to use `bootloader.is_enabled(flag_name)` for Blueprint/Dev Studio/Gallery. PERSONA-1b held off (doc drift between persona matrix and feature-flags rule for `pipeline-exploration-simple`).
- **PERSONA-2** (current): added `qa` persona template (`config/ui/templates/qa_template.yaml`) — all flags ON, ghost_save OFF for deterministic testing.
- **DATA-2** (current): added meaningful tier2 transforms to `MLST_with_metadata` so users can visually verify T1 vs T2 difference (era-derived column + `year ≥ 2023` filter).
- **DECO-1** (`d533c0d`): programmatic audit of viz_factory vs plotnine 0.15.3 → no breaking changes from polars 1.39.3 → 1.40.1.
- **DECO-2** (`3a3437a` + `3ee3b8a`): wrapped 17 plotnine components (scale_alpha/size/shape/linetype family, scale_color_hue/fill_hue, scale_color_continuous/fill_continuous, xlab/ylab/ggtitle/annotate). 38 pytest cases + 17 integrity manifests. README + visualisation_factory.qmd updated.
- **`351dabd`**: hue tuple-coercion fix (mizani requires tuple, YAML produces list).
- **`3cc8490`**: hyphen persona IDs in sidebar_nav (was using underscores → silently failed every gate). Added jinja2 + promoted pyarrow to top-level deps.
- **EXPORT-1** captured: single-plot export (`export_graph_enabled` flag) is deferred Phase 22; bundle covers everything.

### Verified clean

- App import: clean
- Pytest: 90/90 pass (38 DECO-2 + 31 connector + 21 filter operator)
- Demo manifest `1_test_data_ST22_dummy.yaml`: full plot grid renders including Cat 🐱 / miaou violin

### Known issues for next iteration (non-blocking for demo)

- STATE-1 / STATE-2 (=AUDIT-4): active plot flickers on T3 toggle / panel switch / Compare T2/T3 — same family of reactive scoping bug
- PROP-2/3 + UX-NOTIF-1: alerts/inventory persistent panel (bell button preferred design)
- PERSONA-1b: doc drift on `simple` persona's Comparison/Session Mgmt visibility
- Phase 24-A: t3_recipe_engine extraction still pending (deferred post-demo to avoid refactor-during-demo-week risk)

---

## Session Block 5: Phase 24 protocol + Playwright test infrastructure (2026-04-30)

**Objective**: Design the Phase 24 refactor execution protocol and install headless Playwright smoke tests so every Home Theater change can be verified automatically.

### Phase 24 execution protocol

- Created `.antigravity/knowledge/refactor_protocol_phase24.md` — step-by-step execution discipline for the `home_theater.py` decomposition (ADR-051).
  - Pre-flight: `git tag`, baseline capture, 90/90 gate verification.
  - 5 extraction steps (24-A through 24-E) with exact line ranges, risk levels, and change-manifest discipline.
  - Two-commit rule per step: move (commit N) then cleanup (commit N+1).
  - 7 hard rules (no-exception), stopping conditions, public surface declaration.
- Created `.antigravity/tasks/tasks_phase24.md` — actionable step-by-step plan with function-level granularity for each extraction.

### Playwright smoke test infrastructure

- Installed `playwright` (1.59.0) and `pytest-playwright` (0.7.2) into `.venv`; added to `pyproject.toml` `[project.optional-dependencies] test`.
- Created `app/tests/conftest.py` — `shiny_app` fixture via `shiny.pytest.create_app_fixture(scope="module")`.
- Created `app/tests/test_shiny_smoke.py` — 12 tests across 4 tiers:
  - T1 Startup (3 tests): loads, no startup errors, project load renders nav
  - T2 Persona masking (4 tests): 2 always-run, 2 `@pytest.mark.skipif` for non-developer personas
  - T3 Filter pipeline (4 tests): highest-risk refactor surface — form renders, add row, apply no-crash, reset
  - T4 Data preview (1 test)
- Fixed `app/tests/test_reactive_shell.py` — was broken (missing imports, no fixture). Now has proper `shiny_app` import; its 2 tests remain pre-existing failures (persona_selector not rendered in UI) — not regressed.

### Debugging notes (non-obvious pitfalls for next agent)

- `#persona_selector` does not exist in the rendered UI — persona is env-var only at launch. Replaced runtime-switch tests with `@pytest.mark.skipif(_LAUNCH_PERSONA not in (...))`.
- Group nav tab labels come from manifest `description` field (not dict key) — contain emoji. Role-based selectors fail. Use `.nav-link:has-text('Quality Control')` CSS selector.
- `fb_op` detaches from DOM when `fb_col` changes (filter_form_ui re-renders). Must `_wait_shiny()` after column select.
- `gt` op only available for numeric columns. Must navigate to AMR/Mlst Bar plot (uses `MLST_with_metadata` which has `year`) before selecting `fb_col = year`.
- `libs/generator_utils/` and `libs/utils/` have pre-existing ImportErrors — always exclude from pytest invocations.

### Documentation updated

- `rules_ui_dashboard.md §6` — new section: Headless Playwright Smoke Testing (mandatory gate)
- `ui_manifest_integration_testing.md §3` — updated with concrete commands and pointer to rules
- `tasks.md` — PLAYWRIGHT-1 marked [x] DONE
- `implementation_plan_master.md` — Phase 24-E @verify gate expanded with exact Playwright commands
- `.antigravity/knowledge/refactor_protocol_phase24.md` — smoke test commands in verification gate

### Gate state

- 90/90 unit tests pass
- 10/12 smoke tests pass (2 persona-skip — expected)
- App import: clean
