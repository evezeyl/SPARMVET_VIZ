# Audit Log — 2026-05-03

**Session scope:** Gallery UX polish, CSS harmonisation, sidebar toggle fix, recipe_meta standard format

---

## Changes

### Gallery sidebar: icons, dynamic choices, select-all alignment

- Removed duplicate `icon=` kwarg from both `ui.accordion_panel()` calls in `build_sidebar_ui()` — the emoji in the title string IS the icon; bslib's `icon=` was adding a second Bootstrap icon alongside it.
- Taxonomy sub-panel labels changed to `🏷️ <Title>` format (`🏷️ Family (Purpose)`, `🏷️ Data Pattern`, `🏷️ Difficulty`).
- Recipe accordion title: `"🖼️ Recipe"` → `"👨‍🍳 Recipe"`.
- "Select all" row: moved right-aligned (`margin-left: auto` on inner flex container); added `.gallery-select-all-row` CSS class to scope out the global `#nav_sidebar .shiny-input-container { margin-bottom: 4px }` rule that was creating a gap above the checkboxes.
- `gallery_handlers.py` sync handlers (`_sync_family_all`, `_sync_pattern_all`, `_sync_difficulty_all`) now read choices from `gallery_index.json` pivot via shared `_pivot_choices()` helper — same dynamic source as `build_sidebar_ui()`. Hardcoded fallback lists kept for when index is absent.

### Gallery theater: collapsible panes + layout cleanup

- `split_viewer_layout()` rewritten: both the technical preview tabs and the educational guidance pane are now wrapped in separate `ui.accordion()` (`open=True`) — collapsible but open by default.
- Removed the `ui.hr()` between the view-title-banner and the split viewer in `render_explorer_ui()`.
- Removed the `ui.div(style="height: 10px;")` structural gap between the two panes (replaced by `gap-2` on the flex column).
- CSS section 14 added for gallery theater accordions: preview accordion flushes navset card to zero padding; guidance accordion uses the soft-note yellow palette throughout.

### CSS theme.css restructure (sections 13–17)

- Section 13 renamed: "Gallery Sidebar & Filter UI" — filter/sidebar CSS only.
- Section 14 (new): "Gallery Theater (Preview + Guidance accordions)".
- Old section 15 "Guidance Header Scaling" removed (rules moved to §17).
- Section 17 (new): "Educational Guidance Pane Content" — all `.gallery-md-pane` and `#gallery_guidance_accordion` rules consolidated here with a comment explaining how to override per-deployment (same `@import` + override pattern as `demo_vetinst.css`).

### Sidebar toggle CSS fix

- Root cause: `#nav_sidebar .collapse-toggle` CSS had been applying `position: fixed` to the sidebar toggles, but bslib positions `.collapse-toggle` via CSS Grid + `position: absolute` + CSS variable transitions. When both sidebars are collapsed, the grid transition creates a new fixed-containing-block, causing the fixed-positioned toggle buttons to misplace or become unclickable.
- The previous first-rule selector `#main_layout_outer > .bslib-sidebar-layout > .collapse-toggle` was also wrong — `#main_layout_outer` IS the `bslib-sidebar-layout`; the extra intermediate class never matched, so the left toggle had `position: fixed` but no `top/left` coordinates.
- Fix: removed all `position: fixed`, `top`, `left`, `right` overrides. Only `transform: scale(0.75)` (visual size) and `z-index: 1100` (slightly above bslib's default 1000) remain. Bslib's own positioning logic handles placement correctly.

### Gallery recipe_meta.md standard format

All 13 `recipe_meta.md` files and `recipe_template.md` converted to a new standard:
```markdown
## [Recipe Name]

> 📊 Family · 🔢 Data Pattern · 📈 Difficulty

### Section Heading
…
```
- `# Recipe Metadata: X` → `## X` (h2, not h1; drops the "Recipe Metadata:" prefix noise)
- 3 separate `## Family/Pattern/Difficulty` h2 headings → one `> ` blockquote tag strip
- Content section headings: `## Suitability` etc. → `### Suitability` (h3, clearly sub-headings)

CSS §17 heading scale updated to match:
- `h2` = recipe name: `1.0rem/700` — same visual weight as the "📖 Visual Cookbook: Guidance" accordion header
- `h3` = sections: `0.85rem/700/uppercase` — matches sidebar accordion button density
- `blockquote` = tag strip: `0.78rem`, left amber border (`#c8b86a`), slight inset background

---

## Why

- Gallery sidebar icons: two-icon glitch looked unpolished, was causing user confusion.
- Select-all alignment: "Select all" text was being truncated on narrow sidebar — right-aligned row is both clearer and avoids overflow.
- Dynamic pivot choices: hardcoded lists in gallery_handlers.py would silently miss new families/patterns added to the index — same bug as build_sidebar_ui had before ADR-057.
- Collapsible panes: users want more space for the guidance text when reading a recipe; minimizing the preview frees vertical space without losing it entirely.
- Sidebar toggle: the `position: fixed` override was a well-intentioned attempt to pin toggles to viewport corners but broke bslib's collapse/expand JS when both sidebars were collapsed simultaneously.
- recipe_meta format: `## Family (Purpose): Ranking` renders identically to `## Suitability` — no visual hierarchy between taxonomy metadata and content. The blockquote strip is compact, visually distinct, and machine-parseable for future tooling.

---

## False starts / non-obvious choices

- First fix for select-all used `justify-content-end` on the outer div — this is insufficient because `ui.input_checkbox()` renders a `<div class="shiny-input-container">` wrapper that has its own width behaviour. `margin-left: auto` on an inner container is the reliable pattern.
- First sidebar toggle fix changed the CSS selector (`> .bslib-sidebar-layout >` was wrong) but kept `position: fixed`. This improved left toggle positioning but still left the re-open-from-both-collapsed bug. Needed a second pass to understand the bslib grid+transition containing-block issue.
- Considered keeping `position: fixed` with pointer-events workarounds — rejected as too fragile.

---

## Session continuation — Gallery taxonomy expansion + accordion UI harmonization

### 13 new gallery recipes added

Recipes created (all under `assets/gallery_data/`):
`beeswarm_sina`, `ecdf`, `violin_boxplot`, `qq_plot`, `freqpoly`, `scatter_smooth`, `density_2d_bin`, `connected_scatter`, `bar_grouped`, `error_bar`, `dumbbell`, `stacked_area`, `step_chart`

All were initially authored with the wrong VizFactory layer format and required normalization:
- Layer key `geom:` → `name:` (VizFactory expects `name`, not `geom`)
- Per-layer `mapping:` dict → global `mapping:` at plot level
- Labels block converted from key→string to a `labs` layer
- Per-recipe bugs: `grey70` → `#b3b3b3` (dumbbell), `linewidth` removed (error_bar), `log_mic` pre-computed in TSV (qq_plot), `proportion_bar` y-label layer removed, heatmap_correlation text mapping hoisted, lollipop_advanced segment endpoints moved to global mapping

### 6-axis gallery taxonomy (ADR-063)

Three new taxonomy axes added across the full stack:
- `geom` — primary plotnine geom function name (`geom_violin`, `stat_ecdf`, etc.)
- `show` — what the chart communicates (`distribution`, `ranking`, `trend`, etc.)
- `sample_size` — data volume guidance (`any`, `individual points`, `medium+`, `large`)

**Files changed:**
- All 34 `recipe_manifest.yaml` — `info:` block extended with 3 new fields
- All 34 `recipe_meta.md` — blockquote tag strip extended: `> 📊 · 🔢 · 📈 · ⚙️ · 🎯 · 📏`
- `libs/viz_gallery/src/viz_gallery/gallery_manager.py` — `submit_recipe()` + `rebuild_index()` + pivot dict extended
- `app/modules/gallery_viewer.py` — 3 new `_filter_panel()` calls; all 6 taxonomy sub-panels collapsed by default (`open=False`)
- `app/handlers/gallery_handlers.py` — 3 new "Select all" sync effects; `_update_gallery_options()` rewritten with `_pivot_set()` helper enabling 6-way intersection; empty selection = full set (no filter)
- `assets/gallery_data/TAXONOMY_CHEATSHEET.md` — NEW: icon→axis mapping, allowed values per axis, tag strip format, manifest info block template

### Headless preview generator

`libs/viz_gallery/assets/generate_previews.py` — NEW script. Reads `recipe_manifest.yaml` + `example_data.tsv`, renders first `plot_id` via VizFactory, saves `preview_plot.png`. All 34/34 previews regenerated.

Usage: `.venv/bin/python libs/viz_gallery/assets/generate_previews.py [--only name] [--skip name] [--force]`

### Accordion-first UI harmonization (ADR-064)

**Home Theater:**
- Main Plot Card (`home_plots_body_accordion`) and Data Preview (`acc_home_data`) now use `_collapsible_panel()` helper → `ui.accordion()` open by default. Matches Gallery theater style.
- `acc_home_data` title was `ui.tags.span("Data Preview", style="font-size:0.8em;color:#6c757d;font-weight:600")` — inline style overrode CSS bold/dark policy. Replaced with `"📋 Data Preview"` plain string.

**Blueprint:**
- Blueprint Work Area changed from Bootstrap `data-bs-toggle="collapse"` to `ui.accordion(ui.accordion_panel(...))` — `blueprint_workarea_accordion`.
- Bootstrap collapse cards (Glimpse + Plot Preview) kept but styled: bold text, compact padding `2px 10px`, `border: none`.

**CSS `theme.css` §18 / §18b:**
- §18 global rules: bold 0.85rem on all `.theater-container-main` + `.wrangle-studio-container` accordion buttons; `border: none` on accordion-items; white expanded background; no box-shadow inset separator.
- §18b per-ID flush rules: `#home_plots_body_accordion`, `#acc_home_data`, `#blueprint_workarea_accordion` — each gets `border-radius: 8px; overflow: hidden; box-shadow` on `.accordion-item` so header corners appear rounded. Inner navset-card/pill set to `border-radius: 0` (outer clip handles shape).
- TubeMap accordion normalized (was blue) — `#blueprint_tubemap_accordion` now uses standard `#f8f9fa` / `#1a1a1a`.
- Bootstrap collapse cards: `.wrangle-studio-container > .card { border: none; border-radius: 8px; overflow: hidden }` + `.card-header { border-radius: 8px 8px 0 0; border-bottom: none }`.
- Gallery preview accordion grey bar fixed: `#gallery_preview_accordion .accordion-item { border: none; box-shadow; border-radius: 8px; overflow: hidden }`. Gallery guidance accordion: `box-shadow: none` added (was inheriting Bootstrap default which bled as grey between stacked accordions).

---

## Why (session continuation)

- 13 new recipes: fill gaps in distribution, time-series, ranking, and comparison chart families.
- 6-axis taxonomy: 3-axis taxonomy (family/pattern/difficulty) was too coarse for discovery — users needed to filter by plot type (`geom`), message (`show`), and data volume (`sample_size`) independently.
- TAXONOMY_CHEATSHEET: agents kept using wrong icon assignments; a single-file reference prevents the family/pattern/difficulty icon mismatch from recurring.
- Accordion harmonization: Bootstrap `data-bs-toggle="collapse"` created visually disconnected grey header strip that didn't match Gallery accordion style. bslib accordion is the canonical SPARMVET panel pattern (ADR-064).
- CSS rounding inversions: `border-radius: 0 0 7px 7px` on inner navset-card made only the BOTTOM visually rounded while the top appeared flat — inverted expectation. `overflow: hidden` + `border-radius: 8px` on the accordion-item itself fixes the outer shape.

---

## False starts (session continuation)

- Bootstrap collapse approach for minimize/maximize initially kept from an earlier session — user correctly rejected it: "I do not understand why there was the need for New grey headers". The grey appearance comes from the Bootstrap card structure having a separate `.card-header` element. Fix: replace with bslib accordion matching Gallery style.
- First attempt to fix Bootstrap collapse card rounding used `overflow: hidden` on `.card` alone — works for outer clipping but the inner button still needed explicit `border-radius: 8px 8px 0 0` on `.card-header` to be reliable across browsers.
- `_pivot_set()` initially returned empty set on empty selections — this filtered EVERYTHING instead of nothing. Empty selection must return the full set (no filter); fixed before shipping.

---

## Session continuation — UI colour harmonisation & revert (ADR-065, 2026-05-03)

**Session scope:** Demo-prep UI polish. Revert broken structural changes, apply consistent `#345beb` blue across all 4 views, document CSS conventions.

### Revert: structural accordion changes broke the app

ADR-064 had introduced `home_plots_body_accordion` (collapsible Home plot card) and a new Plot Preview Bootstrap collapse card in Blueprint Work Area (`wrangle_studio.py`). These changes:
1. Removed `#central_theater_tabs` from Python while CSS still referenced it — dead selector.
2. Introduced a rogue "Plot Preview" Bootstrap card in Blueprint that the user did not want.
3. Broke `wrangle_studio.py` accordion structure in a way that caused a crash.

Fix: `git checkout bdf8723 -- app/handlers/home_theater.py app/modules/wrangle_studio.py` restores to the pre-ADR-064 Python state. The ADR-064 CSS (§18/18b) was kept — it is valid and applies to the remaining Python structure. Additionally removed the Plot Preview Bootstrap collapse card from the restored `wrangle_studio.py`.

### CSS `#345beb` colour harmonisation

Comprehensive pass to replace all stale `#0d6efd` (Bootstrap default) and `#1a1a1a` (dark text) colour values with `#345beb` (SPARMVET primary blue) on interactive element labels across all 4 views:

- Nav pill inactive links → `#345beb`
- Theater tab `.nav-tabs` links → `#345beb`
- TubeMap accordion header text → `#345beb` (was `#1a1a1a` post-ADR-064 normalisation; reverted to branded colour)
- `#audit_sidebar .card-header` bg/text → white / `#345beb`
- `#audit_sidebar h5` (Pipeline Audit title) → `#345beb`
- `#audit_sidebar h6` (tier dividers: "Active Logic Stack" etc.) → `#345beb`
- Session delete button → amber `#ffc107` style (matches Pending badge)
- Gallery preview accordion header → `#345beb`
- Gallery guidance accordion header (collapsed + expanded) → `#345beb`
- View title banner `.banner-title` → `#345beb`
- `.wrangle-studio-container .card-header .btn` (Live Data Glimpse) → `#345beb`
- `#audit_sidebar .card` → full `border-radius: 8px` (was `8px 8px 0 0`, flat bottom)

### New developer documentation

`docs/reference/ui_style_guide.qmd` created — per-view element→selector→modifiable reference, colour token table, typography scale, 18-section CSS index, hard constraints callout (5 rules protecting functionality), known dead CSS rules. Registered in `docs/_quarto.yml` Chapter 1.

### Why

- Revert: ADR-064 Python changes were premature — introduced visual regressions and a rogue panel the day before a demo.
- Colour harmonisation: multiple sessions had left `#0d6efd` / `#1a1a1a` in various rules; the app had inconsistent blue shades visible as grey/default-blue text on headers that should have matched the primary accent.
- Documentation: no previous CSS reference existed for new developers — every styling change required reading 700 lines of `theme.css` to understand the selector landscape.

### False starts

- Tried CSS variable `--bs-form-check-input-checked-bg-color` to override toggle checked state — Shiny's Bootstrap build hardcodes the checked colour and does not use that variable.
- Used `.form-check-input:checked` selector — Shiny renders `.shiny-input-container .radio input`, not `.form-check-input`. Correct selectors added to §3 but still lose to Shiny's internal specificity on some input types; left as "best effort" with documentation note.
